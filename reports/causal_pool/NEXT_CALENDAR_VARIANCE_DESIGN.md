# Prospective SPX civil quarter-end risk increment

**Unregistered proposal for a possible wave 15.** Carry forward the bounded novelty audit in [NEXT_CALENDAR_AUDIT.md](/Users/byrons/code/trading-vol/ndx-vol-experiment/reports/sign_memory/NEXT_CALENDAR_AUDIT.md). [Wave 14 is independently verified](/Users/byrons/code/trading-vol/ndx-vol-experiment/reports/causal_pool/SUMMARY.md) with no qualifying candidate. That motivates returning to volatility magnitude and a different input channel; it supplies no evidence for quarter-end predictability. This note constructs no features, targets, support counts, associations, forecasts, or scores and changes no frozen artifact.

## One civil interaction, with strong calendar controls

For origin civil date `t`, derive `n(t)` as the next Monday-through-Friday **civil date** strictly after `t`, skipping weekends only. Use no future observed market row, holiday calendar, or early-close information. Let `E5` indicate that `n(t)` is within its month's final five civil dates, inclusive; let `Y5 = E5 × 1[December]`; and define the sole candidate `G5 = E5 × 1[March, June, or September]`. December belongs to the year-end nuisance control. Month lengths, leap years and rollovers follow Gregorian arithmetic. The nominal endpoint can differ from the next actual traded session; do not repair that mismatch after inspecting outcomes.

The baseline contains the entire 18-column [wave-8 SPX/macro basis](/Users/byrons/code/trading-vol/ndx-vol-experiment/calendar_variance.yaml): intercept; log risk means and downside histories over 1/5/22 sessions; VIX, short-IV shape and VVIX; four entry-weekday indicators; nominal elapsed hours; and all three admitted original CPI/payroll/FOMC plan flags. Add eleven nominal-month indicators, omitting January, plus `E5` and `Y5`: **31 baseline columns including the intercept**. The candidate adds only `G5`, giving 32 common input columns. It therefore tests a quarter-end interaction conditional on ordinary month-end, December year-end, additive annual seasonality, and the full existing market/macro information.

Center only the **new civil columns**—eleven month indicators, `E5`, `Y5`, and candidate `G5`—on their arithmetic means over the exact admitted training rows and use fixed scale **1**. Apply those means unchanged to application rows. Preserve the original training transforms and penalty geometry for all 18 old columns, including population scaling of the old weekday/macro flags and their existing nonzero-scale gate; the intercept remains one. This is a newly fitted richer baseline, not a replay of wave-8 issued forecasts. New-civil-column support and constant-input admission rules remain pre-registration decisions below.

## Same risk target and one staged slope

Reuse the existing raw-SPX target `y_t = max(GK[t+1], 1e-10) + log(O[t+1]/C[t])²`, where `t+1` is the next observed SPX session. This is a daily OHLC risk proxy in native squared-log-return units, not measured high-frequency variance. Its availability is that next session's close. Retain the full SPX source calendar, strict rolling windows and previous-observed-session market cutoff. Original macro plans retain the stricter publication-civil-date cutoff and coverage rules for all nominal-window months; ambiguous or reissued sources remain unknown. No unknown plan becomes zero and no macro control is removed to recover observations.

At each first feature-complete monthly application origin, use expanding, common, mature training rows with availability no later than its preceding observed SPX close and at least **1,000** admitted observations. Construct the application schedule before future query-label filtering, retain all scheduled fits, and use the same complete sample for all three models. The historical-mean control is the arithmetic mean target on those exact rows. Fit the positive baseline on all 31 columns using mean proper variance loss and slope penalty `.01`, with an unpenalized intercept. Divide both targets and baseline predictions by the positive training target mean for optimization; restore native forecast units using that same mean.

Freeze that fitted baseline risk function `h0`. With `z = G5 − mean_train(G5)` and fixed scale one, fit only the scalar `b` in `h_b = h0 exp(b z)`, **without an added intercept or refitting baseline coefficients**. The objective is

`F(b) = mean(log(h0/mean_train(y)) + b z + (y/h0) exp(−b z)) + .01 b²`.

Thus `F′(b) = mean(z[1 − (y/h0)exp(−b z)]) + .02b` and `F″(b) = mean(z²(y/h0)exp(−b z)) + .02 > 0`. Positive finite inputs give a unique convex scalar optimum; `b=0` exactly reproduces the baseline. For an exactly constant `G5`, centering gives `z=0` and the penalized objective has the unique optimum zero. Whether such a fold is nevertheless inadmissible for the scientific support requirement must be decided before counts, not after a failed fit. A bracketed derivative root and independent derivative/curvature checks are a practical candidate certification approach; concrete numerical settings are not yet registered.

## Two comparisons and fixed prospective gates

If registered as wave 15, compare the quarter-end candidate against **both** the richer baseline and the same-row historical mean. The only primary score is `log(h)+y/h`, over every retained origin, with absolute mean candidate-minus-control decrease at least **0.005** in both phases and negative differences in both fixed evaluation slices. This is a paired natural-log-score threshold, not a percentage of a possibly negative raw score or a trading-profit calibration. Both comparisons must pass all gates; no month-specific or event-only claim is included.

Retain all **121** prior hypotheses and add two, giving **123**. Allocate wave Holm2 at `.05/(15×16)` and cumulative Holm123 at `.05`, using strict corrected-p cutoffs. Retain 21/63/126 blocks, HAC126, the maximum-p method/phase convention, and 99,999 bootstrap draws, with seed **20260921** and `seed + phase_code×10000 + block` (development 0, evaluation 1). Minimum attainable p `1/100000` is below one tenth of the strictest two-comparison raw wave cutoff `1/9600`.

Keep development origins 2016-01-04–2019-12-31 with label availability by 2019-12-31; evaluation 2020-01-02–2025-10-17; fixed evaluation slices 2020–2022 and 2023–2025; source/target end 2025-10-20; and the sealed period beginning 2025-11-03. Any registered admission, fitting, scoring, verification or publication failure must retain both new hypotheses as unevaluable with p=1. Causal timing and multiplicity accounting do not make this reused history untouched validation.

## Decisions still required before registration

- Fix minimum training and phase/slice support for the civil groups and their complements, rank/identification checks, and constant-indicator handling. Audit date/source-only feasibility after fixing those rules; never weaken them in response to observed coverage.
- Pin baseline and scalar solver details, stopping and independent-certificate tolerances, search/bracketing limits, and failure rules for nonfinite or unsupported underflow/overflow operations. Fix same-unit scaling and stable paired-loss arithmetic tests before any numerical construction.
- Hash-admit unchanged source/protocol dependencies; prewrite civil-calendar edge, nominal-holiday, future-calendar mutation, training-centering, unknown-month, label-maturity, common-cohort, scalar derivative/zero-nesting, and whole-family failure tests. Independently reconstruct features, forecasts and inference before publication.

No source acquisition is proposed. Existing archival Yahoo/Cboe, early VIX9D back-calculation, unverified historical release latency, current-document-vintage and original-plan coverage limitations persist. No exchange expiry, index rebalance, institutional-flow or causal quarter-end mechanism is asserted. The proposal tests only whether this fixed civil interaction improves the specified risk forecast under those controls and limitations.
