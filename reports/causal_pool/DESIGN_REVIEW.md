# Pre-run design and implementation review

The registered question is the fixed causal pool from the frozen wave-13 prospectus. The root runner, recurrence, independent verifier, and scientific plot are separate new modules. Every earlier source/test file, protocol and publication remains unchanged. No empirical filter state, candidate forecast or score was calculated during implementation.

## Independent responsibilities

- A modeling reviewer implemented the recurrence only after prewriting synthetic timing, seed, missingness, endpoint, underflow, application-cohort and immutable-baseline tests. The root separately read the implementation.
- A second reviewer implemented explicit weighted-sum verification, separate probability/score replay, upstream proof reconstruction, inference and failure reporting. The modeling reviewer independently checked the mathematical verifier and ran nine relevant synthetic tests without editing it.
- A third reviewer checked the complete runner/protocol and constructed separate-scale plots plus a synthetic pipeline joining pinned inputs, application selection, private parquet roundtrip, explicit-state proof, root inference bookkeeping and plot admission. It covers a wholly unscored first month, feature gaps, and a boundary label that later becomes mature.
- The root wrote pre-run contract, class-support, effect/family gate, input-snapshot and whole-family publication-failure tests before the new runner. The complete entry point also receives a synthetic temporary-tree success and coherent-state-tampering failure check in the independent test module.

## Findings corrected before any empirical run

Initial missing-module failures were preserved for the prewritten tests. Modeling tests needed two fixture repairs: positional missing-label assignment had added an unintended row, and an entirely unscored month should not call a nonempty Brier helper. A root inference fixture initially retained the old arbitrary candidate probability; the exact convex-pool validator correctly rejected it. The fixture now uses the literal half-sum. No implementation guard was weakened.

The runner review caught a separate-read JSON identity problem inherited from earlier scaffolding: decoded inherited p-values and the later recorded file digest could describe different bytes. A prewritten registered-pin test first failed against that API. The new runner now reads once, checks the admitted hash before decoding, records that same hash, and passes the admitted inherited rows into evaluation. The independent new layer likewise binds metadata and output decoding to snapshots. Frozen older code was not edited.

The independent state comparison has a prospective operation-count roundoff envelope, with strict domains and exact zero/sign masks. Saved quotient, half-product pool, copied baseline and Brier losses require exact float64 replay. The mathematical review found no blocker in the seed exponents, post-seed observation weights, two-session origin-to-memory maturity or continuous reference clock. A final prewritten regression caught complex-valued states being cast to real by a frozen generic scalar helper; the new independent layer now rejects complex scalars and audit columns before that helper. Its initial failure is preserved. No arithmetic formula or tolerance changed.

## Required final gate

The full repository tests, focused pre-run checks, scoped lint and all prior hashes must pass before registration. Their exact counts, timing and code/protocol/design hashes are recorded in the freeze record and run manifest. Registration retains all 119 inherited hypotheses and both new comparisons; any later admission, numerical, scoring, verification or publication failure leaves both additions unevaluable with p=1. No alternate model, tolerance, memory length, weight or excluded fold may repair a failed empirical trial.

No passing research claim can rely on the descriptive average gap or on a favorable single control, period, slice or test method. Both component comparisons and all predeclared gates are required. Reused archival history remains exploratory.
