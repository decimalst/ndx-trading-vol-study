# Read-only prefit integration review

The initial review found no material cohort, count, inference or publication mismatch in the event-cluster runner, protocol and supplied-table pipeline/verifier contracts. A later metadata-only prefreeze check exposed a source-proof schema defect that the synthetic fixture had reproduced incorrectly; its correction and regression evidence are recorded below. This is a bounded prefit review, not empirical approval. The corrected metadata closure and repository-wide rerun must pass before freeze and registration.

The review read source code, protocols, design documents and synthetic test definitions. It ran only a static identity check confirming that the current YAML equals the runner's complete literal contract, its seven anchors equal the admission module's map, and its `original_index` equals the unchanged wave18 YAML index. No actual source admission, market/feature/target/forecast table decoding, new event/support counting, association calculation or historical fitting occurred. No implementation was edited. The one authorized documentation correction describes shared source bookkeeping accurately.

## Cohort and interface checks

The [runner](/Users/byrons/code/trading-vol/ndx-vol-experiment/src/event_cluster_search.py) receives the unchanged original wave18 records from the checked wave19/wave18 admission wrapper. It passes `loaded['protocol']['index']` to the [pipeline](/Users/byrons/code/trading-vol/ndx-vol-experiment/src/event_cluster_pipeline.py), whose original replay validates all old training/application/support records and both preserved controls. The full verifier entry passes the same original configuration as argument six to the [independent pipeline helper](/Users/byrons/code/trading-vol/ndx-vol-experiment/src/event_cluster_verification.py). The remaining five arguments are panel, fits, states, memory and support, matching the implemented signature.

All original train and application windows require six finite event-history coordinates before the first new fit. A missing event history fails the complete trial, without altering the original masks or choosing a later fit date. The history includes mature known labels from unissued and feature-incomplete origins, as explicitly declared. Candidate input is raw adjacent-pair fraction C/21; excess adjacency is preserved only as a diagnostic. Independent reconstruction checks every full-calendar memory value, unknown mask and structural date before any independent new-stage solve.

Historical baseline offsets apply the saved current-month baseline to that month's exact mature training cohort; they are not retrospective historical issuances. Query offsets replay original issued logits and saved probabilities. Both new stages use training-only centers and fixed scale one. Old baseline fitting is not called. Original baseline and recent-frequency scored rows remain exact, and both new arms share their target, availability, phase and fit metadata. Every original unscored application and monthly fit remains represented in state and fit outputs; its new probabilities are validated before scored-row selection.

The runner's scored-panel validation composes both original three-arm validators with the same two controls. It additionally checks actual reference-calendar predecessor and next-session target identities, training minimum and development maturity fences before inference. The original validators enforce target/availability equality, one-session horizon, monthly fit chronology and constant fit metadata, exact Brier losses, common rows, and the unchanged historical date ceilings.

## Count and inference checks

For n scored origins, a complete panel has 4n rows: 2n new nuisance/cluster forecasts and 2n unchanged baseline/recent-frequency forecasts. A monthly count k means k new nuisance fits and k scalar cluster stages, with zero original baseline refits. The helper separately reports k original monthly fits replayed and 2k independently verified new stages. Application count is independent of scored count. Neither the runner nor the helper assumes a historical sample size before actual admission.

The declared family has 131 inherited hypotheses and three new comparisons, totaling 134. Only cluster-versus-baseline, cluster-versus-nuisance and cluster-versus-recent-frequency enter the new family. The earlier wave15 and wave16 failures retain their existing p=1 entries. The closed early-session feasibility review adds no hypothesis. Successful registration/accounting has three registered, 131 inherited and three evaluated events: 137 ledger events. A caught producer failure reconstructs three unevaluable outcomes instead of evaluated ones; if inherited evidence could not be loaded, it explicitly reports that omission rather than fabricating 131 records.

All three controls must show at least .0005 lower Brier loss in both development and evaluation, negative differences in both fixed evaluation slices, wave Holm3 below .05/(20×21), and cumulative Holm134 below .05. The inherited baseline comparison cannot be promoted by itself, and neither nuisance-versus-baseline nor baseline-versus-frequency is an additional promotion test. Six candidate/control-phase analyses use 18 bootstrap calculations across blocks 21/63/126, with HAC126 and the maximum p across methods and then phases. The same phase/block draw design is used across controls.

The runner explicitly sets inference units to one before reusing the earlier paired-loss helper and replaces that helper's old effect-scale denominator with .0005. Individual Brier losses are nonnegative, so this use satisfies that helper's domain. Differences use the factored identity with checked arithmetic and are validated against individual losses. The minimum phase length is 127, preserving the literal 126 bandwidth. The 399,999-draw resolution, 1/400000, is below one tenth of the strictest raw Holm3 wave threshold, 1/25200. Nominal HAC minimum detectable effects and eight phase/model calibration summaries remain descriptive.

## Immutable inputs and terminal publication

All seven new source anchors and the exact original index were checked for static protocol agreement. Source traversal includes the complete wave19 success/publication envelope, the original wave18 verified outputs, recursively declared preservation maps and the closed source-feasibility audit. The verifier shares that metadata-only traversal while independently checking literal anchors and rehashing the declared files; it does not claim an independently implemented source parser. New numerical reconstruction is independent. [SOURCE_DESIGN.md](/Users/byrons/code/trading-vol/ndx-vol-experiment/reports/event_cluster/SOURCE_DESIGN.md) now states this implemented boundary explicitly.

The runner registers all three comparisons before fallible input enumeration or admission. Its initial protocol bytes determine both the parsed specification and registration hash; freeze and complete code/design/test-log identities are checked before and after pretests. The final boundary rechecks freeze, all code/input/preserved hashes, the protocol and both successful upstream failure-marker absences. Recoverable report, evaluated-ledger or identity failures overwrite canonical metrics with three p=1 unevaluable outcomes, retain interrupted ledger bytes and keep any earlier scored diagnostics explicitly unpublished. The independent entry snapshots all eight new output byte buffers before decoding them, checks the helper's complete reconstruction and inference, rehashes outputs and dependencies, and repeats old/new failure-marker checks before writing success.

The plot loader requires a successful verifier and the exact verified metrics hash. A figure or report may not promote scores from a failed or pending trial. Actual empirical output inspection and final visual review remain later publication tasks.

## Preparation evidence and remaining boundary

Owned admission tests initially passed 21 synthetic contracts; the producer pipeline passed ten in 35.149 seconds; the independent pipeline helper passed ten in 42.201 seconds. Each new module retained its preimplementation missing-module red log, and scoped lint passed. The helper's acceptance test forbids new producer calculators and the old optimizer. Its tamper tests cover coherent changes to a finite unscored probability, fitted coefficients, original control bits, memory diagnostics/dates/unknowns, support and monthly cohort records.

This review inspected 15 root search, 13 publication and four plotting test definitions, including registration-before-admission, typed literal-contract mutations, both Holm gates, .0005 equality, bandwidth/resolution, late failure markers and atomic failure-ledger recovery. It did not rerun those suites or certify the still-finishing full-entry test suite. Repository-wide success, final literal/code hash freeze, actual source admission, empirical support and independent numerical verification remain required. Shared archival sources, uncertain original publication latency, back-calculated early VIX9D, a threshold with overlapping history and adaptive reuse of the evaluation years remain limitations even if the trial later passes.

Reviewed byte identities, before final freeze:

| File | SHA-256 |
|---|---|
| `event_cluster.yaml` | `824a9beda7cf293568dd2d16bbd3b76a8235f359a78c764147d75731c22b734c` |
| `src/event_cluster_search.py` | `ffec79f1a1453c58f24c2af99ac03423cb484f0b2183c5711345b5696cc5b9c7` |
| `src/event_cluster_pipeline.py` | `88fb5b5f70718b5ced0ac689d4341c006d4c4a3932fdb19c2bc19b3f9678d469` |
| `src/event_cluster_verification.py` | `8c969e9e51217376fb7c32149e448d432ff34aebb25cf8cf1c48981cf11f3244` |
| `tests/test_event_cluster_search.py` | `fa83c63c153e83f70620ce36abcd31871351dcf43b9249a9903ceb1774c94927` |
| `tests/test_event_cluster_publication.py` | `19278ff232c848e188a98701418909f279f129ce9a330cb835183c1f92166c14` |
| `reports/event_cluster/DESIGN.md` | `89e310189ccd3d31218fab952c5905037f388805e470fc1fd22e827999d65127` |

## Prefreeze metadata-schema correction

After the initial 2,136-test repository pass, but before any freeze record, registration or new historical table decoding/history/support/fit, the parent's real metadata-only collection failed at the latest verification ledger field. The actual immutable wave19 proof stores `ledger_events_verified={'inherited':129,'registered':2,'evaluated':2}`. The initial synthetic admission fixture and draft checker mistakenly treated that field as scalar 133. The publication audit's distinct `ledger_events` field is scalar 133; its `ledger_event_counts` is the role map. The earlier review and synthetic fixture had not exposed this shape mismatch.

The new [regression red](/Users/byrons/code/trading-vol/ndx-vol-experiment/reports/event_cluster/admission_ledger_schema_regression_red.txt) reproduced the failure using the exact frozen map. Only the new draft admission and its synthetic fixture changed: verification now requires the exact typed role counts; publication total/map and the actual admitted ledger's role counts retain their original checks. No dual-shape fallback, count relaxation, earlier artifact edit or numerical-model alteration was made. All [22 admission tests passed](/Users/byrons/code/trading-vol/ndx-vol-experiment/reports/event_cluster/admission_ledger_schema_regression_green.txt) in 1.955 seconds, and all [11 affected independent admission/full-entry tests passed](/Users/byrons/code/trading-vol/ndx-vol-experiment/reports/event_cluster/verifier_ledger_map_compatibility_green.txt) in 0.805 seconds. Scoped admission lint passed.

Corrected draft identities are `src/event_cluster_admission.py` SHA-256 `a835775d0c9bbd23abea61484b93e5f675458b969ae6319f7f8391600468f4d9` and `tests/test_event_cluster_admission.py` SHA-256 `61de4a6d29a9147a2d83a0bd22d535f6f1a8b4ccb35fdec5b561c93831ab4c21`. The earlier green log is retained; it is not the required final check of these corrected bytes. The corrected real metadata-only collection has now completed with **1,933 pinned paths**. [metadata_readiness_audit.json](/Users/byrons/code/trading-vol/ndx-vol-experiment/reports/event_cluster/metadata_readiness_audit.json), SHA-256 `1f3885eece452c8a0fe2835c1168eb6b9920056d29df4cf377af378138234d4e`, records the exact original role counts and total 133, zero new registrations, no numerical-table decoding and no new history/support/fit/score construction. The full repository rerun remains pending. Prior proof bytes, empirical gates, tolerances, objectives and information sets remain unchanged. This was a pre-registration source-schema correction, not an empirical failed trial or a new hypothesis.

No other unresolved material integration issue was found within this bounded review. This statement does not replace the final metadata, full-suite or historical verification gates.
