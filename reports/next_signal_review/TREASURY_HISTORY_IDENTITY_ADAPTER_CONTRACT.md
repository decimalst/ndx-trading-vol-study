# Adapted auction identity pass

The previous goal turn made progress by completing the three bounded exception reviews and saving the terminal preservation checkpoint. This pass implements their observed rules in separate modules and preserves the initial identity report, all original documents and all prior predictive results. It registers no new predictive comparison.

The term parser reads bounded announcement coupon syntax without converting coupon values, and recognizes the visually confirmed split `Mont h` token. PDF and both XML term/type descriptors must still agree. The calendar helper retains actual dates and accepts exact anniversaries or explicit same-month calendar month ends. Reopenings derive original tenor from original dated-to-maturity dates. Distinct current/original dated dates are permitted only on the confirmed substitution branch; final confirmation still has to match actual CUSIP, offered term, original issue, tenor and series.

The PDF reader authenticates own-security fields in the observed row or fourteen-cell column layout. Supplemental STRIPS identifiers cannot replace the own CUSIP, and duplicate, empty or conflicting own fields fail. Every amended title remains explicit. A separate reviewed amendment record binds event, unchanged archive/XML announcement date, actual PDF release, original PDF/text hashes and the associated dated primary notice. Ordinary date conflicts, unreviewed amendments and unsolicited amendment records fail. All metadata inputs remain unchanged, and the output keeps original term/date lexemes alongside reconciled descriptors.

Generated tests were written and run to failure before implementation. Independent review additionally identified an omitted filename-date guard and conflicting bare `CUSIP:`/`CUSIP#` labels; separate failing regressions preceded their fixes. A runner review added an explicit required pin for the prior identity result. All required source maps, exact four-role inventory, confirmation review inputs, amendment review inputs and per-event raw/text hashes are verified before application and again before publication.

The new run retains all 1,138 selected events in the original order, the documented contingency exclusion, missing documents and unavailable notices. No financial values are converted. Successful metadata reconciliation is not source admission or predictive qualification. The new checkpoint is `TREASURY_HISTORY_IDENTITY_CHECKPOINT_V3.json`; the new output is `TREASURY_HISTORY_IDENTITY_V2.json`. The cumulative predictive family remains 144.
