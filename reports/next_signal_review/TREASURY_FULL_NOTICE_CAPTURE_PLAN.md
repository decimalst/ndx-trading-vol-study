# Capture and review the complete known nominal special-notice set

The previous goal turn made progress: twenty fixed-pilot result pairs passed accounting and lineage checks, all seven pilot special URLs were reviewed, and the prior frozen research remained unchanged. No new predictive comparison or qualifying signal resulted. This turn advances the same dealer-allocation source investigation.

Use exactly `TREASURY_FULL_NOTICE_METADATA_SELECTION.json`, SHA256 `7f1fb67ffb6b8543d40ec15fd51d54cea76bf89575dfa32cac14ecae17599971`. It collects all 253 distinct linked special PDFs for the existing six-term nominal metadata universe: 235 provisional auction rows from 2010 through 2025-10-20. Seven originals were already captured and reviewed; reuse their exact bytes, receipts and source paths. Request the other 246 URLs once, retaining every full-inventory association. A reused notice may have more full-inventory associations than its pilot membership; do not discard those additional relationships or claim its pilot scope was exhaustive.

Before requests, independently reconstruct the selection from the 16 pinned projected annual metadata files, verify all source pins and known originals, and validate all URLs, dates, counts and memberships. Generated acquisition tests precede implementation/application. Use certificate-verified HTTPS without redirect following, three workers, a 45-second transfer limit, a 50-second subprocess limit and an 8 MiB PDF limit. Save private original bodies, receipts, headers, errors and per-document summaries. Refuse existing output directories/reports; preserve individual transport failures and continue independent requests without retrying failed or completed transfers.

Transport success establishes captured PDF bytes, not release authenticity or source admission. Under a separate pinned text-inspection checkpoint, reuse the seven already extracted pilot texts and extract the new PDFs with the bundled pypdf runtime. Keep all full text private. Apply the already tested release-header guard before semantic/source-quantity use. Missing, conflicting, post-ceiling or unsupported headers remain explicit review cases; the filename is not an authenticated release date. Unknown header extraction requires visual resolution from the original, retaining the prior parser failure.

A generated-test-backed lexical fingerprint may group documents for review by replacing date values, security identifiers and digit runs while preserving all other words, negation, punctuation and order. This reduces repeated reading of boilerplate only. It never merges events, authenticates a release clock, resolves actual applicability, proves that an amendment changed the same fields, or admits an auction. Each original URL and its own metadata and evidence remain in the ledger. Differences or ambiguities must remain visible; a generic keyword match is not a completed classification.

The independent full-notice review contract governs purpose, actual cross-event applicability, corrections versus first addenda, conditional substitutions, schedule changes, contingency tests and original recovery. Review the entire relevant text, with whole-page visual checks for corrections, tests, uncertain dates, layout dependence and ambiguous cases. Template-assisted classification still requires per-document identity/date/applicability verification and evidence references. Keep explicit cross-references beyond the API's linked rows; do not fetch unselected documents merely because they are mentioned.

The existing pilot included actual source figures and numerical source accounting. This new batch initially produces only transport/text/metadata review evidence, not financial field conversion, model features, targets, forecasts or empirical scores. The 2025-10-20 numerical source ceiling and protected market observations remain intact. Full known-notice review alone will not complete admission of the auction history, offering-size controls, original-tenor exceptions, source coverage or release support. The comparison family remains 144 until a later separately frozen predictive experiment.
