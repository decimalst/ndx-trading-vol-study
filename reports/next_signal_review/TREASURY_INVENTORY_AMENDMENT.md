# Archive display-bucket amendment before numerical admission

The first metadata capture is terminal `REQUIRES_SOURCE_REVIEW`, not a predictive null. Its unchanged artifact is `TREASURY_ARCHIVE_CAPTURE.json`. The certificate-verified request returned HTTP 200, 483,079 bytes, SHA256 `a4f1eb0410d6295ead217ea97a42110ab30584ca825ec50212247868884bebfa`. All row dates passed the full-response preflight; projection stopped at an empty `t3a` display grouping.

Metadata-only diagnosis found 301 returned records, including 51 cash-management bills with an exactly empty display grouping. Each has a CUSIP, security type, security term, announcement/auction dates, and document fields. Every supplied filename follows the predeclared grammar. The six proposed regular nominal coupon groups each have 12 entries in this 2010 inventory; that is current-inventory metadata, not proof of original-document completeness or statistical support.

The amended inventory contract permits an **exact empty string in `t3a` only** and preserves it as an exact empty `term_bucket`. Missing, null, numerical, or whitespace-only `t3a` remains invalid. All other field, date, numerical quarantine, duplicate, transport and URL checks stay unchanged. A display grouping is neither an inferred original term nor a numerical signal. No record is dropped or assigned a replacement term.

This documented schema amendment occurs before reading any source auction values or outcomes and before any predictive registration. It does not change the proposed target, feature, candidate universe, clock, support threshold, score or acceptance gate. The 2010 response and receipt will be reused byte-for-byte, rather than downloaded again. Remaining requests retain the original bounded 2011-through-October-20-2025 intervals.

The initial projector, verifier, tests, script, checkpoint and terminal capture remain unchanged. New inventory modules, prewritten generated tests and a second metadata checkpoint record the revised schema. This is a continuation of source-format inspection, not a new predictive comparison. The registered family remains 144.
