# Offering PDF identity-label amendment

This separate prospective adapter addresses the preserved first offering pass: one exact agreement, 19 failures at `Unique labelled announcement CUSIP required`, and two prior exclusions. The checkpoint SHA-256 is `b6e0783a74746b363c7464fd9f2444f5af1e7f951e49a74f9e7e25f67c64b699`. The prior helper, tests, script, checkpoint and outputs remain unchanged.

Only CUSIP-containing lines of the 19 affected retained announcement texts were inspected. The checkpoint and its metadata/selection pins were checked, then each text was checked against its recorded hash. Every affected text has an own-security `CUSIP Number`, separate `Corpus CUSIP Number`, and `CUSIP Number(s)` supplemental label. The last has a value, `None`, or no same-line value. The frozen broad starts-with-CUSIP count incorrectly includes this plural label. The Corpus line itself does not start with CUSIP and was not the extra counted line.

The new `src/treasury_offering_layout.py` retains the original parse and reconciliation interfaces and exact output schema. It checks precisely one nonempty `CUSIP Number` against the caller's own-document identity. Only the observed `CUSIP Number(s)` label is exempted from the starts-with-CUSIP ambiguity guard. Corpus and plural-label values cannot supply or substitute for the security identity. Unknown CUSIP-leading labels, wrong own identities and duplicate or empty own labels remain failures. No text is removed, replaced or rewritten; the original text also passes through the existing dated-header check. All prior title, auction-date, size and encoding checks remain.

The adapter reuses the frozen XML token checks and exact integer-dollar decoder. The fixed 1e9 XML mapping remains a hypothesis checked against the independently dollar-labelled PDF for every pair; neither the schema nor this amendment authenticates universal XML units. All document identities are checked before amount conversion. Caller-authenticated same-offering linkage remains necessary, including original/final identity substitutions. `source_admitted` remains false.

Eight generated test methods are written before implementation: separate corpus/plural identities, wrong or missing own identity despite a matching supplemental value, duplicate/empty/unknown own labels, exact output equality with the frozen helper on unaffected fixtures, fixed financial mappings and strict failures, identity-before-conversion and separately expected final identity, intact text transport, and unchanged title/date/financial-row strictness. Fixtures are invented. Genuine RED and final GREEN/lint evidence are retained separately. No historical offering reapplication is performed by this task; root owns a new checkpoint and separate result artifact.

The missing-module RED exited 1. The first implementation passed all eight tests in 0.004 seconds; scoped Ruff passed. Evidence is in `TREASURY_OFFERING_LAYOUT_RED.log` and `TREASURY_OFFERING_LAYOUT_GREEN.log`. No test expectation or parser adjustment was needed after implementation. The old helper's rejection of the generated supplemental-label fixture is itself checked, preserving the distinction between the frozen first pass and this explicit amendment.
