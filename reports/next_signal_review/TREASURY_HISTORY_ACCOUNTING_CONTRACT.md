# Full-history quantity reconciliation

Apply only to events whose exact saved original announcement/result inputs pass the final paired identity reconciliation. Preserve every other event and its explicit prior disposition. The event with an unavailable required notice remains unknown; it is not converted into a zero or silently removed from historical donor pools.

Validate the XML's separately announced and final identities, then parse the frozen set of sixteen result quantities with the original exact accounting arithmetic. Independently parse the competitive result PDF's own identity, bid-to-cover footnote, high yield and two accepted/tendered tables. Require equality of all sixteen quantities. Parse announcement offering size from its explicit dollar row or the tested fixed column cell and compare it exactly with both XML offering fields using the prospectively fixed billion-dollar conversion. PDF amendment release dates and original XML announcement dates remain separate authenticated contexts.

The runner binds all four raw/text inputs to the successful identity report, checks the full original event order and retains all stage failures and parsed values in private per-event outputs. A complete match requires both result parsers and the offering reconciliation to succeed, with no mismatched result field. Partial success remains a review state. The public report contains only event identities, statuses, field names and artifact hashes; financial quantities remain in the ignored private directory.

The initial outputs and source-specific correction notices remain intact. There is no corrected backfill, inference from accepted amounts to offering size, optimization over scale, or new predictive comparison. These source checks precede the complete availability ledger, unknown-history masks and prospective modeling registration. Required artifact pins and the checkpoint itself are rechecked before publication; replay cannot overwrite an existing source run.
