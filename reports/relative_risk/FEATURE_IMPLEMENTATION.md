# Relative-risk source and feature implementation checks

Completed before any empirical numerical measurement audit, feature/target construction, fit, or score. Only the new producer/tests and this wave's reports were written; earlier source, code, tests, and reports remain unchanged.

`tests/test_relative_risk_features.py` was written before `src/relative_risk_features.py` existed. The initial test command exited 1 with `ImportError: cannot import name 'relative_risk_features' from 'src'`; the original subprocess output is retained in [features_initial_red.txt](/Users/byrons/code/trading-vol/ndx-vol-experiment/reports/relative_risk/features_initial_red.txt). The initial 24 synthetic tests passed. Independent review then identified an exactly constant, nonzero Pearson-window corner case: centering repeated `0.1` values could leave rounding residuals and incorrectly yield finite correlation. A new test was written and failed for either constant asset and for both; its output is retained in [nonzero_constant_initial_red.txt](/Users/byrons/code/trading-vol/ndx-vol-experiment/reports/relative_risk/nonzero_constant_initial_red.txt). Before any empirical measurement, the producer added an exact original-value constancy guard, with no variance epsilon or clipping. **All 25 synthetic tests now pass**, and scoped Ruff checks pass. The final successful output is retained in [features_implementation_checks.txt](/Users/byrons/code/trading-vol/ndx-vol-experiment/reports/relative_risk/features_implementation_checks.txt).

The synthetic contracts cover the exact 27-control/28-common-column order, one-session market lags, native intraday and total-risk mean histories, negative-return histories, both source predecessor identities, strict missing-window propagation, centered 22-pair Pearson correlation, zero-denominator unknown values, fixed correlation roundoff tolerance without clipping, current-entry weekday, next-session label maturity, signed/zero relative targets, raw-price unit invariance, ignored adjusted-close fields, future-value and future-calendar invariance, and bounded source parsers containing forbidden post-fence tokens.

The full-reference measurement gate is tested separately from feature completeness. A zero or small-positive raw GK observation anywhere on the SPX reference calendar fails the complete study, including a leading row that could never enter training. Missing OHLC rows remain unknown and are counted without removing reference dates. Invalid OHLC and nonfinite computed GK raise errors. The audit distinguishes nonpositive values from small positive floor hits, and an asset with no observed measurements cannot pass by vacuous comparison. `build_features` repeats the measurement gate defensively before constructing any predictor or target; the root runner first persists the standalone measurement audit, then invokes `require_measurement`.

The producer imports the frozen SPX/VIX/VIX9D/VVIX source parser read-only, adds bounded QQQ raw OHLC and VXN parsing, and preserves the full bounded QQQ source history for predecessor identity. Its source audit updates the earlier study-specific description without altering earlier audit files. Forecasting, monthly maturity admission, optimization, gate accounting, and independent empirical verification belong to the root runner and verifier.

| Artifact | SHA-256 |
| --- | --- |
| `src/relative_risk_features.py` | `0170b5e2b4b5957abebefc797d2274c45f9e1bb7b7ab18e97d659b63ae6eb1d3` |
| `tests/test_relative_risk_features.py` | `b2ccbfcc48d06527786d294adb2f6fd9acb8a618caf6f467378021635750c501` |

The earlier [date/source feasibility result](/Users/byrons/code/trading-vol/ndx-vol-experiment/reports/relative_risk/SOURCE_FEASIBILITY.md) remains a date-support result: it does not establish that the unrun full-history numerical measurement gate will pass. No empirical numerical values were inspected to prepare these tests or this implementation.
