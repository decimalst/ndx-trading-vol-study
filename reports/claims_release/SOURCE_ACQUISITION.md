# Claims source acquisition checkpoint

2026-09-08. All claim observations remain quarantined. No historical observation token has been converted to a number, no market-aligned source cohort has been built, and no new predictive trial is registered.

The current ALFRED form offers Zipped CSV rather than the older tab-delimited format described by its help page. The verified public HTTPS form was fetched with system curl. Python's default trust bundle first failed certificate validation; an installed certifi bundle validated the connection but the HTTP response timed out. TLS verification was never disabled.

The first POST returned only the filled form because the named download button was absent. Adding the observed `form[download_data]` field yielded a ZIP. Its README omitted only the unreliable initial2009-05-28 archive snapshot, which the prospective parser design already excludes. The final request explicitly begins with the reference week2009-05-30 and vintage2009-06-04, ending observation range2025-10-18 and selecting only vintages through2025-10-20. Its853 metadata vintage dates exactly match the README; the last is2025-09-25. These are vintage metadata counts, not admitted observation or market-support counts.

The final private raw response is `data/claims_release/source_metadata/alfred_initial_claims_quarantine_v3.response` (SHA-256 `4d182029a97b16b48a116168f8191d30bd769520182b2f578625c4b71a65ac4d`). The two members are `README.txt` and `obs.,_initial_release_only.csv`. The exact header is `period_start_date,ICSA,realtime_start_date`; the README explicitly identifies ICSA, original Number units, weekly Saturday reference dates, seasonal adjustment and initial-release-only output. Previous transport attempts, requests, response headers and metadata are retained privately.

All25 prewritten generated parser tests pass. They caught an omitted acquisition-clock validation in the first implementation before any historical value admission. The loader now checks an explicit UTC acquisition timestamp after the requested source dates, alongside the archive, identity and complete-row date checks before numeric conversion. The original failure log remains retained. Source parsing still cannot certify original release authenticity; separate DOL archive and release-text checks are being built.

The `realtime_start_date` field will remain separate from the DOL public-release date. The timing review establishes that ALFRED may source-date this field; it is not an independently certified ingestion timestamp. The preliminary feasibility wording that assumed such a second clock is superseded for source admission by this distinction. The eventual forecasting origin rule remains to be frozen after source authentication.

Official documentation: [download form](https://alfred.stlouisfed.org/series/downloaddata?seid=ICSA), [output metadata definitions](https://alfred.stlouisfed.org/help/downloaddata), [date provenance](https://alfred.stlouisfed.org/help).

## Subsequent controlled source access

The quarantine-only statements above describe the original checkpoint. At 2026-09-08T20:55:25Z, the tested loader first converted the bounded export's claims values. The exact attempt, source/request pins and output identity are retained in `FIRST_STRUCTURAL_ATTEMPT.json` and `FIRST_STRUCTURAL_RESULT.json`. There are 852 observed records, reference weeks 2009-05-30 through 2025-09-20, with four missing requested reference weeks at the end: 2025-09-27, 2025-10-04, 2025-10-11 and 2025-10-18. The initial snapshot had already been excluded from the request. These are source counts, not market support or predictive evidence.

Before this access, the bounded review identified a README defect: a second contradictory attribute row could follow the expected units/frequency/seasonal-adjustment row. Two new generated tests, including six failing invalid-span subcases, demonstrated it before correction. The loader now validates the complete attribute spans through the Notes boundary. All 67 source-component tests pass on the corrected files, with code hashes recorded in `SOURCE_COMPONENTS_GREEN.json`. Documentary Notes continuation remains supported.

The repository-wide run started before that correction and completed with 2,443 passing tests in 379.682 seconds. Its receipt correctly has `unchanged: false` because the prospective source fix occurred during that run; it is not a certificate for the final source code. The corrected source-component check is separate, and a full final-code regression remains required before any predictive registration. The one-shot admission wrapper initially resolved the prior publication's report-relative paths against the repository root and stopped on a missing file before creating an admission receipt or decoding source counts. Correcting that wrapper path resolution allowed it to verify all 336 prior frozen Python files and 90 peak-age report artifacts unchanged before the actual source attempt.

The five date/format-selected original reports now match ALFRED exactly on reference week, value and release date (`PILOT_RECONCILIATION.json`). The annual archive's three inconsistent links have documented official-body resolutions (`ARCHIVE_RECONCILIATION.md`). Its complete requested-year metadata still does not prove complete weekly reports. Comparing date inventories found a missing October 17, 2019 archive link, an extra Saturday March 15, 2014 link, and a March 22, 2018 release absent from ALFRED's date set. ALFRED assigns the March 17 and March 24, 2018 reference weeks to the same March 29 date; this is an explicit reconciliation issue, not two independently authenticated first-release dates.

All 852 bounded archive-linked bodies have now been acquired successfully (844 new verified HTTPS responses and eight pinned reuses), with every request recorded. The independently located official October 17, 2019 DOL PDF is a separately pinned coverage supplement. Full text extraction and week-by-week reconciliation remain in progress. No source-ready decision, market join, numerical forecasting feature, model fit, empirical predictive score or new hypothesis registration has occurred.
