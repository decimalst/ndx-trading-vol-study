# Bounded acquisition of original DOL release documents

Started 2026-09-08 from the audited archive ledger at `data/claims_release/dol_resolution/acquisition_ledger.json`, SHA-256 `f3833c175b312c079b9f865768b81d00ec9ed3391c311ef117a616bf0d9053f6`. That ledger represents the annual archive indexes and preserves documented identity exceptions. It does not establish complete weekly publication coverage or authenticate numerical first-release values.

The private, one-shot acquisition plan was written before requests at `data/claims_release/dol_bulk/plan.json`, SHA-256 `e3690783c9c7171fa88e12919255629951045573ce3ae92728f4180faa3b0ffd`. It binds the script, source ledger, original index/resolution inputs and all reused body/header/receipt bytes. Its 852 records are sorted by release date and URL and include every archive-listed release from 2009-06-04 through the 2025-10-20 ceiling. Selection does not depend on an ALFRED match. The archive-only 2014-03-15 and 2018-03-22 records remain included, and the documented 2019 filename exception retains its actual observed URL.

Eight exact-URL responses were reused from the pinned pilot and resolution captures; 844 new public HTTPS GETs were completed. Reuse copied the unchanged original body and headers and retained the original full receipt plus its source reference and hash. The acquisition used system curl with verified TLS, at most two concurrent requests, a 15-second connection timeout and a 45-second total transfer timeout. Each new URL received one attempt, without automatic retries or redirect following. The script records HTTP errors and transport failures explicitly and continues the remaining queue; none occurred in this run.

All outputs stay in the ignored private directory `data/claims_release/dol_bulk/`, with directories mode 0700 and files mode 0600. Each release receives raw body/header bytes, the complete curl transport receipt, stderr and a structured source receipt. The script only saves and hashes body bytes; it does not extract document text, parse claims values, construct source support cohorts, or run models. Existing files and frozen source modules/tests are unchanged.

The already captured, independently located 2019-10-17 release is outside this fixed archive-list plan and is handled as a separate coverage supplement. It must not silently change the saved 852-record plan or be described as a newly archive-listed link.

All 852 planned releases were successfully captured by **2026-09-08 21:01:09 UTC**: 844 new HTTP-200 responses and eight reused pinned HTTP-200 responses, with verified TLS and zero failures. Raw body bytes total 325,243,073. Every input pin was unchanged after the queue finished, and all private output permissions passed the check.

The flat downstream ledger is `data/claims_release/dol_bulk/acquisition_ledger.json`, SHA-256 `ba8f5919f5e7e211d506616d48980e48a8bce101d47bf655cc4530232747087d`. Each record exposes `release_date`, `release_url`, `body_path`, `body_sha256`, `receipt_path`, `receipt_sha256`, `status`, `verified_tls_http_200`, header/curl receipt pins and the original archive record. Reuse provenance remains explicit. The complete transport summary is `data/claims_release/dol_bulk/acquisition_summary.json`, SHA-256 `f52b58c77e2923ea65546310b0d3167e4ec0d5692372c5e2aeb5c0949cd8fef2`.

Successful transport alone is not numerical source admission. No document text or claims count was extracted in this task; the next independent document extraction and first-release reconciliation remain separate work. No predictive experiment was registered or run.
