# Bounded prefreeze integration review

This review uses only new/frozen code, protocols, design documents and synthetic test evidence. It did not admit actual historical tables, compute event histories/support, fit models, inspect retained probabilities/losses or run empirical inference. The complete independent publication entry was still being finalized during the initial review. One concrete final-boundary issue was reported to both owners before freeze; its disposition is recorded below.

## Source and reconstruction wiring

The [runner](/Users/byrons/code/trading-vol/ndx-vol-experiment/src/event_cluster_replay_search.py) consumes the new admission's exact three-tuple `(audit, original_loaded, retained)`. The [source wrapper](/Users/byrons/code/trading-vol/ndx-vol-experiment/src/event_cluster_replay_admission.py) returns the original wave18 dictionary unchanged and exactly six retained wave20 objects: forecasts, states, memory, fits, support and upstream admission. These retained objects remain explicitly unverified inputs. The complete failed-wave20 preservation envelope and the still-successful wave19/wave18 proofs are separate roles in the combined audit.

`reconstruct(loaded, retained)` passes the eleven helper arguments in the declared order: original features, targets, panel, fits, states, original wave18 index configuration, then retained panel, fits, states, memory and support. Original configuration is argument six in both the runner and independent helper. The [independent full entry](/Users/byrons/code/trading-vol/ndx-vol-experiment/src/verify_event_cluster_replay.py) additionally compares the loaded original index configuration to the new protocol's exact original-index section.

The runner calls full `reconstruct` before `evaluate`. It saves only new upstream-admission and reconstruction-audit JSON; it does not reserialize the retained Parquet or fit artifacts and does not call a producer forecast generator or optimizer. Independent nuisance/scalar solves validate the saved objectives and predictions; their optima do not replace retained coefficients or forecasts. A reconstruction failure enters the whole-trial p=1 failure path before inference.

All original monthly training/application cohorts and unscored applications remain subject to the frozen helper checks. Full target-clock event history, exact unknown masks, original source geometry, original issued controls, saved probabilities, support dictionaries and monthly fit metadata are reconstructed before independent new-stage solves. Independent primitive Brier checks remain within reconstruction; no scored summary or hypothesis inference is admitted before the full helper completes.

## Exact scope of the technical change

A parsed comparison of old and new YAML confirms that `original_index`, `history`, `model`, `models` and `verification` sections are exactly equal. An AST comparison of the two `verify_pipeline` functions finds only the replacement of the final application-state exact-frame comparison with `compare_state_dates`, plus returned audit/count labels. Every original cohort/history/primitive-score/numerical-stage statement is unchanged.

The new [date helper](/Users/byrons/code/trading-vol/ndx-vol-experiment/src/event_cluster_replay_verification.py) operates on temporary copies of seven declared application-state datetime columns. It accepts native timezone-naive ms/us/ns dtypes only, requires known values to be midnight, compares unknown masks exactly, converts losslessly to ns with a checked roundtrip, and compares exact instants. Overflow, subday/timezone/object values, changed instants and unknown masks are rejected. The frozen exact-frame check then applies to all other values, dtypes, indices, names, shapes and order. Full memory/index/panel comparisons receive no new exception. Inputs are not modified.

The [11 prewritten helper tests](/Users/byrons/code/trading-vol/ndx-vol-experiment/tests/test_event_cluster_replay_verification.py) cover original synthetic sources explicitly stored in ms, us and ns before any generated fit. They roundtrip original/retained tables through Parquet and audits through JSON, then call the unmocked independent reconstruction and stage solvers. The ms/ns cases falsify the unchanged old helper before its solve loop and pass the new helper. They preserve final and boundary unscored applications and reject date/numeric/coefficient/coherent-unscored-probability tampering. This directly tests the transport boundary missed by the earlier default-us fixture; it does not establish that actual historical replay or later numerical verification will pass.

## Counts, inference and failure accounting

For n scored origins and k retained monthly schedules, reconstruction reports 4n retained rows, consisting of 2n candidate/nuisance rows and 2n original controls; k original monthly schedules replayed; 2k independent stage optima checked; and the separate full application/state count. **Newly generated forecasts and new producer fits are both zero.** The runner's metrics preserve this naming. Successful repeated independent verification is not a second generation of those retained forecasts.

The new family is 134 inherited plus three newly registered contrasts, totaling 137. The three old wave20 canonical p=1 rows are included once among the inherited records. Earlier failed families and the zero-hypothesis documentary feasibility audit remain preserved. Only cluster-versus-baseline, cluster-versus-nuisance and cluster-versus-recent-frequency can contribute a new lead; all three controls are required.

A successful new ledger has three registered, 134 inherited and three evaluated events: 140 total. This coincidentally equals the old failed ledger's length, but its role map is different: the old record has three registered, 131 inherited, three evaluated and three verification-failed events. The source layer preserves the old record and its opaque scored middle lines. A new producer failure keeps three new unevaluable p=1 outcomes, with inherited records included only if admitted. A later independent-verification failure appends three verification-failed events to the new evaluated record and makes all three canonical new outcomes p=1; the success-length total must not be reused for that failure history.

The new wave allocation is exactly `.05/(21*22)` and cumulative alpha remains .05. All controls require Brier improvement at least .0005 in both phases, negative differences in both fixed evaluation slices, wave Holm3 and cumulative Holm137. The 399,999 draws give resolution 1/400000, below one tenth of the strictest raw wave cutoff, 1/27720. The fixed seed and original dependence settings remain declared. The runner uses unit-one paired Brier inference and overrides the older helper's effect-ratio denominator with .0005. Six phase analyses, 18 bootstrap calculations and eight descriptive phase/model calibration summaries remain distinct from the three-hypothesis family.

## Input and publication boundaries

The new literal protocol is parsed from a single initial byte snapshot. Freeze/code/design/test-log checks occur before and after pretests. Three registrations are appended before fallible source collection and input enumeration. The complete source closure, inherited canonical metrics, new freeze evidence and previous artifact inventory are pinned before actual source admission. The source layer checks all retained buffers before decoding and repeats the exact required failed-wave20 marker check after its final rehash, while failures in successful wave18/19 remain forbidden.

The runner's scored publication rechecks freeze, full code/input/preserved hashes, protocol and failure states. Its atomic ledger recovery preserves interrupted bytes and canonical three p=1 outcomes; missing inherited evidence is disclosed rather than fabricated. The new independent entry snapshots the new audit/metrics/ledger outputs, repeats source and retained reconstruction, compares the saved reconstruction audit under the declared numerical tolerances, independently recomputes inference and ledger identities, then rechecks output/dependency identities before success. Shared metadata traversal is explicitly disclosed and separately rehashed; numerical reconstruction is independent of the producer.

The figure loader requires successful new verification and exact verified metric bytes, preserves the old failed record and rejects failed current/successful-source markers. Its three-control figure retains literal uncertainty intervals, the fixed .0005 threshold and exploratory-history language. Actual figure inspection remains a post-verification publication task.

## Concrete finding and preparation status

The initial runner and independent-entry review found that their final general hash loops included the old wave20 failure marker, but did not freshly repeat its required binding after the loop. A mutation/removal occurring after that marker's loop iteration could evade the final marker boundary even though the admission helper had checked it earlier. The successful-ancestor absence guards did not cover this required failed object. Both owners were notified before freeze. No root or verifier source was edited by this reviewer.

The runner owner prewrote six cases that mutate or remove each old failure/metrics/verification file after its general-loop visit. [All six failed the original draft](/Users/byrons/code/trading-vol/ndx-vol-experiment/reports/event_cluster_replay/terminal_guard_regression_red.txt). The new `require_bound_failed_attempt` now checks all three exact manifest input hashes, regular-file presence and no symlink immediately before appending evaluated events. [All 16 publication tests passed unchanged in 0.935 seconds](/Users/byrons/code/trading-vol/ndx-vol-experiment/reports/event_cluster_replay/terminal_guard_regression_green.txt). Read-only review confirms the guard is after the final general loop and before the ledger commit.

The independent-entry owner separately retained [an expected-red late-loop mutation test](/Users/byrons/code/trading-vol/ndx-vol-experiment/reports/event_cluster_replay/entry_final_marker_initial_red.txt), then added `require_bound_failure(root, closure)` after the final closure hash loop. That rereads the exact old failed trio and repeats successful wave18/19 failure-marker absences, followed by the new replay's absence check before proof publication. [All 18 entry tests passed in 1.226 seconds](/Users/byrons/code/trading-vol/ndx-vol-experiment/reports/event_cluster_replay/entry_complete_green.txt). The identified boundary issue is closed within this reviewed implementation. Neither correction changes a historical failure, accepted numerical tolerance, predictor, objective or hypothesis gate.

The owned source tests passed 20 contracts in 2.376 seconds, including an analogous source-layer marker regression retained as red before its fix. Root search/publication/plot evidence reports **37 tests passed in 1.140 seconds** in [root_formatted_checks.txt](/Users/byrons/code/trading-vol/ndx-vol-experiment/reports/event_cluster_replay/root_formatted_checks.txt). The helper's unchanged-contract final run reports **11 tests passed in 49.310 seconds** in [verification_final_green.txt](/Users/byrons/code/trading-vol/ndx-vol-experiment/reports/event_cluster_replay/verification_final_green.txt). This review inspected those test definitions/logs without duplicating their executions.

The parent's [metadata-only readiness audit](/Users/byrons/code/trading-vol/ndx-vol-experiment/reports/event_cluster_replay/metadata_readiness_audit.json) reports **2,053 pinned paths**, zero new registrations, no retained numerical-table decoding, zero independent historical stage solves and no replay inference. This is a documentary closure count, not new empirical predictive support. This reviewer checked that audit's schema and contents without rerunning the actual collector.

No other material integration mismatch was found in this bounded scope. Final owner readiness, full repository tests and freeze remain prerequisites to historical replay. Successful replay would still reuse the same adaptively selected archival observations; it would neither change wave20's failed status nor constitute fresh predictive evidence, a new sample, causal self-excitation identification or trading validation.
