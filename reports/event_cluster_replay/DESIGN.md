# Wave21: technical verification replay of event clustering

Prospective specification, 2026-09-07. This replay is unregistered. No retained historical forecast table has been decoded by the new implementation, no historical independent stage solve has run, and no replay inference has been computed. The original wave20 failed record remains immutable and unevaluable.

## Question and retained attempt

Wave20 attempted to predict the original SPX daily risk alert using adjacency among its last22 mature event labels. Its producer completed, but independent verification stopped on equal cutoff dates represented in different storage units. The failure occurred before independent stage optimization and inference. A separate date-only diagnosis and synthetic construction established that string reparsing produced microseconds while source timestamps retained milliseconds. That explains the recorded failure; it proves no other mathematical output correct.

Wave21 asks whether the exact retained producer output can complete all independent checks under a newly declared, lossless application-state date comparison. It does not propose another fitted feature, target, sample or forecasting model. It retains every original prediction and parameter for independent challenge, rather than producing replacements. There are zero new producer fits and zero newly generated forecasts.

The original mathematics remains fixed: full-calendar22-arrival history; the five event-count, endpoint, expectation and recency nuisances; raw adjacent-pair fraction C/21 as the sole fitted candidate; original market baseline replay; a free nuisance intercept and five .01-penalized slopes; a .01-penalized scalar on frozen nuisance offsets; training-only fixed-scale centering; current-fit training offsets and original issued query controls. The excess-adjacency statistic remains descriptive only. Every original cohort and unscored application remains required.

## Narrow representation change

Only the seven explicitly listed date columns in the final full application-state comparison receive semantic unit equivalence: origin, feature cutoff, source fit origin, first/last arrival dates and first/last source-origin dates. Other frame and index comparisons remain exact under their original contracts.

Accept native, timezone-naive datetime64[ms], [us] or [ns] columns only. Both sides must retain exact row/index/column structure and unknown masks. Known dates must be normalized midnight values. Convert comparison views using checked exact nanosecond arithmetic, reject overflow or rounding, and require lossless conversion back to each original unit. Compare the resulting instants exactly. Keep all source/phase/calendar/maturity checks.

There is no blanket dtype waiver, string coercion, timezone removal, date truncation, tolerance or altered timestamp. Every nondate value and dtype remains subject to its existing exact check. Original artifacts retain their bytes and storage units. The new helper records the compared fields, original units and successful lossless checks.

Prewritten synthetic tests must generate original source records in ms, us and ns; run the original and retained producers on those invented data; serialize/reload all relevant tables; and call the unmocked independent mathematical verifier, including its new-stage solves and unscored application. The ms/ns cases must demonstrate that the original frozen checker still fails. Changed dates, subday times, timezones, object columns, NaT masks, overflow, reordering and numeric dtype/value changes must fail the new comparison as appropriate. A successful date check alone is never model verification.

## Source identity and registration order

Admit the failed wave20 attempt as the object to be checked, explicitly unverified. Pin its six retained producer outputs, its canonical failed metrics/verification, full ledger, protocol, freeze, test evidence, failure review, diagnosis and publication-preservation audit. Preserve unpublished scores opaquely; they neither choose the replay nor supply its inference. Where no successful old output certificate exists, the new source envelope identifies the retained current bytes and says so.

The original wave18 and wave19 source/control proofs remain required successful ancestors. Their failure markers remain forbidden. The exact identified wave20 failure is required and hash-bound. The new wrapper cannot ignore arbitrary failures or reinterpret wave20 as verified. Decode only snapshots whose bytes were checked, and repeat complete identity and failure-marker checks at publication.

After the full repository suite and freeze, register all three new comparisons before numerical admission or reconstruction. Then admit checked originals and retained outputs. Run the entire independent history/cohort/support/state/model reconstruction before computing any replay score inference. This independently solves retained nuisance and scalar optima, but never replaces a saved coefficient or forecast. Save that reconstruction audit, recompute replay statistics from the retained panel and publish provisional replay metrics. A separate guarded verifier repeats the source/reconstruction checks and independently reconstructs inference before issuing any successful proof.

The source metadata traversal is shared, with independent literal-anchor and complete-file hash checks disclosed. New mathematical reconstruction uses the frozen independent objectives and solvers against the separately implemented, retained producer output. The independent entry uses the existing separate bootstrap/HAC/statistical implementation rather than accepting the runner's calculated results.

## Fixed family and accounting

Retain134 prior hypotheses, including all three failed wave20 comparisons at p=1. Add exactly three replay comparisons for137 cumulative: clustering versus original baseline, matched nuisance and original recent frequency. A normal new ledger contains three registrations,134 inherited and three terminal evaluations or unevaluable records:140 events. A subsequent verification failure preserves attempted evaluation history and appends all three failure outcomes; canonical failed metrics control interpretation.

Every control must show at least .0005 lower Brier loss in both original phases, negative differences in both fixed evaluation slices, Holm3 below .05/(21*22), and cumulative Holm137 below .05. Keep the seed20260926, phase/block offsets,399999 draws, blocks21/63/126, HAC126 and conservative maximum p-value rules. The minimum Monte Carlo p1/400000 remains below one tenth of the strictest raw wave cutoff1/27720. Calibration and uncertainty summaries remain descriptive.

Report retained candidate/control forecast counts, retained monthly schedules, original baseline fits replayed, independently checked stage optima and all scored/unscored applications separately from zero new producer fits or generated forecasts. The runner and verifier each reconstruct retained stages; repeating verification does not create new predictive observations or independent datasets.

Any source, representation, support, optimizer, forecast, inference or publication failure leaves all three replay hypotheses unevaluable at p=1. No historical repair, alternate solver or changed tolerance follows a failure. The failed wave20 proof and its original three p=1 entries remain unchanged on either outcome.

## Interpretation

A successful replay would certify the exact retained output under wave21's declared representation rule. It would not retroactively pass wave20, create a fresh historical dataset or establish untouched out-of-sample replication. The original daily risk proxy, overlapping threshold, source-vintage limitations and protected period beginning2025-11-03 remain. Predictive attribution, if supported by every gate, would be relative to the declared models rather than proof of an exogenous information channel, physical regime or trading edge.

The separately reviewed trailing-peak-age return idea remains unregistered and unscored. This replay completes the currently unanswered technical verification question first.
