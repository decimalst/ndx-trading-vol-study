# Source-complete civil quarter-end SPX risk replay

This sixteenth wave repeats the quarter-end forecasting question that wave 15 could not score. The earlier attempt failed when an immutable staging tree omitted four required document snapshots. A subsequent metadata-only audit found those four snapshots and one documentary capture manifest, all with matching hashes already bound by pinned ledgers. Wave 15 created no new civil features, support counts, fits, forecasts or scores. Its two p=1 outcomes are retained, and its protocol, code, reports and terminal files are unchanged.

The new protocol follows the prior frozen source-complete replay prospectus. Its five added document paths and expected hashes are declared explicitly before registration. The run inventory includes both successful upstream inventories, the failed attempt's full inputs and reports, and those five documents. After registration, an independent source-closure check follows the selected final CPI/payroll ledgers, original annual FOMC rows and coverage, and the final payroll artifact map. It validates each metadata byte snapshot before decoding and each source document before text parsing. Bounded excluded/reissued/ambiguous BLS records still require their source evidence. Out-of-fence records are excluded before snapshot access. Paths must remain within the root, active hash declarations must agree, and no missing or altered file is repaired.

The retained superseded intermediate payroll amendment records historical provenance. Its former hash for the current ledger pathname is not an active binding or an alternative accepted digest. The complete registered documentary corpus remains unchanged; active dependencies follow the frozen reader and selected final metadata only.

The source package is reconstructed twice through separate closure implementations. The original wave-8 source reader receives all original input and inherited metric bytes plus the checked active closure at the same relative paths in a temporary tree. Only documentary source_path prefixes are normalized for exact audit comparison. The full original calendar-variance and causal-pooling proof records must match their pinned VERIFIED records. A separate proof checks the failed wave-15 protocol, manifest, freeze record, reproduced admission failure, p=1 metrics, 125 ledger events and empty output directory. No prior writer is invoked and no old inventory is compared against the expanded current code tree.

The mathematical experiment is unchanged. The target is next-session SPX max(Garman–Klass,1e-10) plus the squared raw overnight log return. The nominal predictor date is the next civil Monday through Friday after the origin, ignoring exchange holidays and early closes. The baseline contains the old eighteen market/macro columns, eleven nominal-month indicators, ordinary final-five-civil-date month-end and December year-end controls. The only increment is the final-five-date interaction for March, June and September. It adds one penalized centered slope to a frozen fitted baseline. The other control is the common-row arithmetic training mean.

The frozen wave-15 feature, model and score modules are imported through explicit pure-function interfaces. Their lag, rolling-history and original-plan masks, expanding monthly schedule before query-label filtering, maturity cutoff, at-least-1000 training rows, civil group support, rank and conditional identification gates, mixed geometry, penalties, deterministic optimizers, numerical domains and stable proper-score arithmetic remain fixed. Independent civil arithmetic, trust-exact baseline fitting, bracketed scalar root finding and normalized saved prediction replay reuse the corresponding frozen independent mathematical functions. No module global is patched.

Both comparisons must reduce mean log(h)+y/h loss by at least .005 against their respective controls in both development and evaluation, improve in both fixed late slices and pass all multiplicity gates. The 123 inherited comparisons include the failed attempt's two p=1 rows. Two new registrations create 125 total. Wave Holm2 uses .05/(16*17); cumulative Holm125 uses .05. Inference retains HAC126, block widths 21/63/126 and 99,999 draws, with new prospective seed20260922 plus phase_code*10000+block. Minimum attainable bootstrap p=1/100000 is below the first raw wave-Holm cutoff .05/544; unlike wave15, it is not below one tenth of that cutoff. No one-tenth resolution gate is asserted. Nominal HAC80% minimum detectable effect divided by .005 is a descriptive power diagnostic.

The source/label bound 2025-10-20, sealed period beginning2025-11-03, development/evaluation dates, all numerical thresholds and both control requirements remain unchanged. Any admission, support, fit, score, verification or publication failure preserves both new hypotheses unevaluable at p=1. Complete prewritten tests, scoped lint, old-pin checks and a new freeze precede empirical civil construction. Independent verification precedes interpretation. Existing archival, original-plan, nominal-window and daily-risk-proxy limitations persist; neither source completeness nor reused-history gains establish untouched confirmation or trading profit.
