# Pre-fit review of the source-complete replay

The previous goal turn produced useful failure evidence: immutable wave-15 staging omitted four runtime extraction snapshots and a documentary capture manifest. Both failed hypotheses remain p=1, with no new forecast or support output. The new work addresses that source packaging through a separately counted attempt. Every earlier protocol, source/test file, report and terminal record remains unchanged.

A source reviewer prewrote twenty synthetic contracts before implementing the new active dependency collector. The tests place valid bounded BLS evidence outside the ledger directory, stage its checked bytes and successfully invoke the frozen admission scanner. They cover out-of-fence exclusion before snapshot access, bounded excluded reissues, hash checks before metadata decoding, duplicate/malformed/conflicting declarations, safe paths and symlinks, FOMC coverage identity, the final payroll capture-manifest binding and immutable snapshot use. They do not execute a new empirical source collection.

The root prewrote new runner and publication contracts before implementing the new entry point. They preserve the frozen model and evidence geometry, exact125-family accounting, signed native proper scores, .005 power denominator, civil/source support before inference, and failed comparisons at p=1. Additional contracts require registration of the failed attempt's entire inventory and the extra five documents before closure collection, check the old calendar protocol and new document bindings before decoding or collection, and retain both p=1 outcomes if source closure fails before any optimization. The runner imports the existing feature, model and score modules without editing them or patching globals.

A separate numerical reviewer prewrote the new plot and full generated pipeline tests. The plot pins the wave16 threshold and family125 to the new protocol, preserves the .005 effect boundary, checks every empirical count against verification, and labels native absolute score differences. The generated pipeline exercises the actual frozen monthly fits, complete support/rank/identification audits, independent saved-prediction reconstruction, a whole unknown-source month and a fitted but wholly unscored first application month. Its small bootstrap count is a synthetic test fixture only; it compares all four actual inference phases with a separate implementation and verifies all123 inherited plus two new comparisons. The empirical draw count remains99999 and the new prospective seed remains20260922.

The source reviewer also read the new protocol and runner independently and found no blocking source or numerical-contract mismatch. The .05/544 first raw Holm cutoff is larger than the attainable p=1/100000. The previous wave's descriptive statement about one-tenth resolution no longer holds and is explicitly not repeated. Neither the predictive effect threshold nor the inferential method is relaxed.

The new independent verifier is responsible for a separate active source-closure implementation, complete successful wave14 and wave8 proof reconstruction with the full staged input set, exact failed-wave15 admission, new125-family inference/ledger reconstruction and fail-closed publication. It uses frozen independent mathematical functions through explicit interfaces for unchanged civil, fitting and score arithmetic. Its complete admission and temporary-tree entrypoint tests must pass, alongside the generated integration and full repository suite, before registration.

Initial absent-module and construction-stage import failures are retained as pre-implementation evidence. They are not empirical trials or claims of validated admission. No real civil feature, support count, fit, forecast or score has informed this specification. The final freeze record will capture exact full/focused test counts, scoped lint and all prior-pin checks. Any later failed gate retains the complete new family and does not trigger a same-attempt model or threshold repair.

The independent verifier completed eleven owned synthetic tests, including actual frozen BLS scanning inside the staged old-wave8 proof and the complete new verifier entrypoint. Its final combined run with the six generated mathematical integration tests passed. A separate source reviewer read the completed staging, failed-attempt proof, successful upstream identities, inventory coverage and output accounting and found no material blocker. The full repository suite and scoped new-file lint then passed before the freeze.
