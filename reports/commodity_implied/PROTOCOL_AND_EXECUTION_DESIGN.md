# Prospective protocol and execution integration

The fixed wave-24 protocol and its exact validator were written before any historical commodity features, cohort, fits or scores. Three generated contract tests reject altered information sets, support/effect thresholds, inference choices, source boundaries and family sizes. The validated YAML's SHA-256 is `1f939707fb1e1ad96af2cc11084f3337e3ea4c49053e093feecfae9cde2ad497`. Source admission is an earlier separate checkpoint; the predictive family remains 142 until actual registration.

Eight source-input tests were prewritten before the reader, and eight registration/preservation tests before the runner. Their actual missing-module failures are retained. The first combined 19-test run passed, alongside the three protocol tests. No expectation or numerical rule changed; scoped formatting and import/lambda-binding lint fixes were applied before the prospective freeze. An independent set of 11 run-integration tests then checked actual temporary artifact writes and durable registration with invented inputs; its initial test-fixture failures and final passing evidence are retained separately.

The source inventory binds the completed admission certificate, its pre-admission checkpoint, both original raw and parsed series, the four established market files, and all prior claims artifacts. A read-only compatibility check authenticated 1,538 unique pins and the full 142 inherited rows without numerical source decoding. The established claims freeze's 370 Python files and other prior evidence remain unchanged. The reader additionally authenticates all four commodity byte snapshots and preflights both parsed date columns as text before numerical decoding; the independent source checker reconstructs their exact bounded contents again after registration.

The one-shot runner first performs generated serial-null calibration, independent calibration verification and all repository tests with unchanged code. It then writes a prospective freeze binding the protocol, computational Python code, source/dependency artifacts, preserved prior evidence and prefit checks. The reporting-only renderer is separately bound in the later publication audit and cannot contribute forecasts or inference. Numerical history functions cannot run through this entry point without that completed freeze. Full-suite success requires a unique nonempty success summary, a zero process exit code and at least the 2,644 tests from the prior completed freeze. Buffered diagnostic stdout after unittest's summary does not masquerade as test failure or hide one.

The durable ledger writes two registrations and 142 inherited records before either source reader. The callback receives no authority to rewrite the inherited family, which is separately copied before execution. Saved feature/target panels precede the monthly pipeline; unsupported scheduled fits retain their full coverage and schedules. Source, model, verification or inference failure preserves both new hypotheses at probability one. Existing registrations and output directories cannot be restarted or overwritten. All detailed forecast artifacts remain in a private directory; source inputs are never rewritten.

On success, independent forecast reconstruction precedes inference and independent statistical reconstruction. Every pinned artifact is checked again afterwards. The terminal record binds metrics, ledger and available frozen-test/calibration/verification artifacts for the later report-only renderer. Rendering cannot fit models, rescore predictions or choose a favorable result. A separate terminal/publication audit and visual inspection will verify any reported result after the single registered run.
