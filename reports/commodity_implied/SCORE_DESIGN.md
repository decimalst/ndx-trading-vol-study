# Fixed daily commodity inference and accounting

Prospective component design, 2026-09-08; generated tests precede implementation. No empirical feature, fit, forecast panel, support measure or score is read or computed. Only the new commodity score module, its generated tests and these logs are owned here.

Public APIs are inherit_family(previous, signature), evaluate(panel, calendar, prior, protocol), failure_metrics(error, signature) and calibrate(protocol). The previous completed claims artifact must have status COMPLETED, two claims comparisons in candidate-versus-matched/market order and cumulative142. Preserve its prior140 rows verbatim and append its exact two rows with source reports/claims_release/predictive/metrics.json, the supplied authenticated SHA256 and local row indices0/1. Require literal finite probabilities, complete study/candidate/control/horizon identity, unique source/row-index identity and one consistent valid SHA per source. Old rows lacking status remain unchanged; no missing historical status is fabricated.

The new score panel has exactly three model names and one row per origin/model. All arms must share the same targets, endpoints, phases, original-calendar offsets and any other supplied common metadata. Validate finite positive targets/predictions, exact saved QLIKE y/f-log(y/f)-1, native date identities, the fifth subsequent full-calendar endpoint and modulo5 position before filtering. Development labels mature by development end; evaluation labels by source_end. The source ceiling is immutable2025-10-20. No weekly claims identity or weighting is relevant.

Evaluate candidate-minus-matched and candidate-minus-market QLIKE. Both phases and their support are required for both contrasts. Report ordinary per-origin means, annual calendar/scored/missing counts, all five original-calendar offsets and the fixed two evaluation slices. Each phase requires505 observations, each slice252 and each offset63 under the production protocol. Generated fixtures use smaller declared floors. Preflight support for all phases and slices before resampling; unsupported valid cohorts raise INSUFFICIENT_DATA and never become null findings.

Reuse only the frozen orthogonal_round2 generic circular bootstrap, Bartlett HAC and Holm helpers. Share a two-column bootstrap design across both contrasts. Production blocks21/63/126 each use399999 draws, and HAC uses126 lags. Seeds are20260930+5000000+phase_code*10000+block, with development0/evaluation1. Each centered two-sided plus-one bootstrap probability uses deviations of resampled means from the observed mean. A phase probability is the maximum across blocks and HAC; a hypothesis probability is the maximum of the two phases. Report all block intervals, HAC summaries/nominal MDE and the conservative interval envelope.

Correct the two new hypotheses and all144 inherited/new probabilities separately. Production wavealpha is0.05/(24*25), cumulativealpha0.05. Both controls must pass both corrections, candidate-minus-control phase means<=-0.005, and negative differences in every offset/evaluation slice. No release count or equal-release mean enters a gate. Results have study commodity_implied, candidate candidate, controls matched/market, horizon5 and score qlike. Row verdicts are COMPARISON_GATE_PASS or DOES_NOT_QUALIFY; leads is [commodity_implied] only when both pass. Top-level metrics mirror the claims schema with counts2/144 and common_scored_origins; inherited_rows remain a deep copy.

Phase output keys are name,n,delta,candidate_loss,control_loss,first_origin,last_origin,block_inference,hac,p_conservative,ci95_envelope,annual,offsets,stability. Annual entries are year,n,delta,calendar_origins,missing_origins; offset entries offset,n,delta; evaluation-slice entries start,end,n,delta. Registered failures preserve both p1 rows and whole_wave_aborted with explicit INSUFFICIENT_DATA or INVALID_RUN, without a qualifying lead.

Calibration remains generated-only stationary AR(1), rho0.8,n1500,burn200,200 replications and499 draws per block, with the new seed20260930. Conservative interval-envelope coverage must reach0.90. The result reports every calibration setting, seed, envelope coverage, per-block coverage and the limitation that this null is not financial-data coverage certification. The bounded tests use smaller replications/sample/draws and a separate explicit observation-resampling/HAC oracle. Actual production calibration and full tests remain root-owned before registration.

Implementation evidence: SCORE_RED.log records the missing-module failure before source creation. All12 generated tests passed on the first implementation, then again after formatting in0.121 seconds in SCORE_GREEN.log. Focused Ruff checks pass. The new scorer's fields and settings were checked against the current commodity_implied_protocol source; no protocol or numerical source was changed. `_probabilities(row)` is also exposed for the root runner to validate newly evaluated rows before they acquire inherited provenance; `_prior` always retains strict source-identity requirements.

Stable scorer SHA256: `7935db815a67069593271712eddb984a8691eaf9e49871718435ea99a69e1f2c`. Test SHA256: `e89e4a9ba8652aa8d2689aed8c5345c943fb3f9f8ec0069fb429b86f0494b361`. The read-only templates/helpers are claims_release_score `5787097e47b7103d80c16bc111e0c4d50d2a697d6ac2adb2a7256770e0ace3f5` and orthogonal_round2 `9e8b9f47c2c7f9ef1c8fceb9cda6f910e5f95cfb9dbf1e110161e65440800779`; neither was edited. No production-size calibration, full suite or historical calculation was performed by this component task.
