# Generated run orchestration review

This bounded integration review examines `src/commodity_implied_search.py` and adds generated temporary-root tests only. The production runner already exists; these are pre-empirical integration contracts, not a claimed missing-module RED. No real source bytes, admitted records, market cohorts, features, fits, outcomes or prior numerical reports are accessed. No production file or frozen source-admission artifact is changed.

The tests execute the actual `run` and `execute_registered` orchestration and actual JSON/Parquet persistence against generated temporary files. Source readers, feature construction, targets, model pipeline, forecast verification, scoring and score verification are mocked. Source/freeze authentication helpers are mocked at their explicit boundary; separate pin-validation contracts own authentication details. The score verifier import is temporarily replaced only within each fixture, so these tests do not claim independent numerical verification.

The registration test checks the actual ledger bytes from inside both source readers. It requires exactly two ordered registrations and all 142 inherited records, and confirms successful `fsync` of the ledger inode before either numerical source callback. Success tests require the full stage order, the six Parquet outputs plus fit JSON, separate verification proof, terminal metric/ledger hashes, and private output permissions. Original generated raw-source sentinels are checked byte-for-byte after runs.

Pre-source tests exercise missing/rejected freeze, rejected source pins and existing output/registration refusal. Post-registration source, feature, target, model, forecast-verification, scoring and score-verification failures must retain both registered comparisons at p=1, preserve all inherited records, count 144 total comparisons and report no lead. InsufficientDataError must persist its complete aborted coverage and schedules while returning no successful forecast panel. Verification failures must not leave a success verification proof.

The reviewed control flow validates the prospective freeze and source identities before registration, then durably registers before either bounded numerical reader. All numerical stages execute inside the registered callback and exceptions become whole-wave failure metrics. Pre-registration identity rejection therefore prevents any new numerical attempt, while a source-decoding failure after registration retains both comparisons. This review does not rerun calibration, the full suite, or historical execution, and does not extend into additional publication machinery.

All 11 tests pass in 0.257 seconds in `RUN_INTEGRATION.log`; scoped Ruff formatting and lint also pass. The initial run is retained in `RUN_INTEGRATION_FIRST_RUN.log`. Its five failures arose from two fixture assumptions: replacing the entire `sys.modules` dictionary rolled back newly imported pyarrow extension modules between cases, and Parquet does not retain pandas' inferred BusinessDay frequency attribute. The fixture now replaces/restores only the mocked score-verifier entry, and persisted-frame comparisons require the same dates, values, columns and dtypes without requiring that inferred frequency attribute. No production code or numerical contract was changed.

No blocking production orchestration flaw was found within the assigned scope. The temporary-root tests demonstrate stage ordering and the failure accounting exercised above; independent source, forecast and statistical verifier contracts remain responsible for their own numerical and authentication claims. Only this new test file, review, and focused logs were written.
