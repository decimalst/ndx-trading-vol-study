# Wave 11: direct residual cross-moment prediction

Specified before any new empirical residual product, forecast product, or product loss. The numerical contract is [cross_moment.yaml](/Users/byrons/code/trading-vol/ndx-vol-experiment/cross_moment.yaml); the independent algebra and admission challenge is [DESIGN_REVIEW.md](/Users/byrons/code/trading-vol/ndx-vol-experiment/reports/cross_moment/DESIGN_REVIEW.md).

The preceding joint-risk study found nominal improvements from changing dependence, but the candidate failed the registered multiplicity gates. Its matrix score can reward changes in dependence that compensate for imperfect marginal variances. This distinct test asks whether the same issued forecasts predict the off-diagonal residual second moment more accurately. It does not revise the preceding verdict.

For each original forecast origin, use the original next-session raw intraday log returns and common issued means. The target is `Y=(r_QQQ-mu_QQQ)*(r_SPX-mu_SPX)`. Each model predicts `q=rho*sqrt(h_QQQ)*sqrt(h_SPX)`. Score `(Y-q)^2`, with negative and zero products valid. Conditional squared loss elicits the mean of this residual product when its expected loss exists. That functional equals covariance plus the product of the two conditional mean errors; it need not equal true covariance.

Register exactly two comparisons: the existing dynamic-correlation model against constant correlation and against the constant residual matrix. Use every one of the original 7,386 model/origin rows across 2,462 scored origins. No new model fitting, forecasts, predictors, source values, selected regimes, weights, clipping, or observations. The conditional pair retains exactly the same issued diagonals and means; all three arms retain the same returns and means. The constant-matrix comparison also changes diagonal forecasts, so its improvement alone cannot isolate the dependence increment.

The exact upstream protocol, successful verification, verifier, and original manifest have literal SHA256 anchors in the new protocol. The new manifest must pin every earlier report, private output, and input before registration and read-only reconstruction. Reconstruction calls only the frozen verifier's reading and checking functions. It never calls an earlier writing entrypoint. New product calculations decode immutable original forecast and calendar byte snapshots only after their hashes match the new registration. Save the admission proof only in the new output directory. Any admission failure retains both new hypotheses with p=1 and leaves the previous experiment intact.

The paired score difference is computed as `(q_candidate-q_control)*((q_candidate-Y)+(q_control-Y))`, after checking each individual squared error. This avoids subtracting the common `Y^2` term. Reject nonfinite arithmetic and nonzero-factor multiplications underflowing to zero; retain true zeros and representable subnormals. A fixed epsilon/downward-ULP rule checks numerical coherence without an absolute tolerance in return units. Independent primitive comparisons also require matching zero and sign masks.

Fix the minimum absolute mean loss reduction at **1e-10 in fourth-power decimal log-return units**. This is `(0.01*0.001)^2`, the squared cross-moment error associated with a 1% by 10-basis-point reference return pair. It is a transparent reference scale chosen before these scores, not measured economic utility, a literal RMSE improvement, or profitability. Both development (2016–2019) and evaluation (2020–2025) must clear it against both controls. The two fixed evaluation slices must both improve.

Use the original daily cohort, phase maturity rules, and source fence of October 20, 2025; November 3, 2025 onward remains sealed. The same existing predictors retain their one-session lag. Normalize paired gaps by the single fixed unit `1e-10` for inference, then restore dimensional estimates. This is a units conversion, not observation weighting. In each phase use 99,999 circular bootstrap draws at blocks 21/63/126 and Bartlett HAC126; take the largest two-sided p-value. Take the larger phase p-value, then apply Holm2 at `0.05/(11*12)` and separately Holm114 at `0.05`. Keep all 112 inherited hypotheses, two registrations, and two terminal outcomes: 116 ledger events.

Nominal HAC 80% minimum detectable effect and its ratio to the fixed threshold are diagnostics only. No added power gate, outcome-dependent scale, pruning, or retry is permitted. Failure to qualify is not equivalence. Product uncertainty depends on cross fourth moments; archival ETF/index data, shared mean errors, fixed issued diagonal forecasts, and reused historical observations limit interpretation. A successful score would support this narrower predictive functional against these controls, requiring fresh-history confirmation before promotion.

Before the empirical run, run the new synthetic scorer, runner, publication, independent verifier, plotting and inference checks, plus full repository discovery. Freeze all source/test files and this protocol. Independently reconstruct all new product cells, stable differences, four phase analyses, 12 bootstrap runs, both Holm families, and all ledger events before interpretation. Keep failed trials and all previous artifacts unchanged.
