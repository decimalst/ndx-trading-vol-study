# Wave 19: causal calibration of issued risk alerts

This adaptively chosen experiment follows the frozen wave-18 prospectus. It asks whether a single changing intercept, learned from genuinely issued baseline probabilities and their later mature outcomes, improves the same next-session SPX risk alert. The older model-memory work already studied issued residuals for continuous risk; this is a narrower calibration rule on reused information, not a new source or a new claim of orthogonality.

“Issued” refers to the immutable earlier walk-forward application records generated by the archival experiment; these are not contemporaneously live-stamped production forecasts.

The target, daily OHLC proxy, strict threshold of twice the preceding 22-session mean, one-session predictor lag, source bounds, complete calendar, original applications, scored cohorts, support requirements and original monthly baseline fits remain exactly those independently verified in wave18. No protected-period data are decoded. Current-vintage archival Yahoo/Cboe inputs have unverified original publication latency and revision history. These limitations remain attached to the new result.

## Fixed forecast rule

Recover each original application logit from the original monthly transformation and saved baseline coefficient, including applications not scored because of evaluation fences. Require replay of its saved issued probability before any calibration fit. Do not invert rounded probabilities or reconstruct an old prediction using a newer fit. Preserve both original controls, including their training metadata and the recent-frequency control's broader stream of known labels.

At the first original application's prior-session cutoff initialize an empty calibration history and exactly zero correction. At each later full-reference-session cutoff, admit only a genuinely issued baseline whose binary label has matured. Never refeed labels available at or before the seed cutoff. Advance age through feature gaps, missing labels, unscored applications and the phase boundary. A missing issued forecast provides no pair; an issued forecast with unknown outcome provides no pair; both still consume time. Cache original applications before consulting their future query labels. There is no warm-up deletion or alternative half-life.

With delta=2**(-1/63), each mature record has unnormalized weight (1-delta)*delta**age. The sole correction minimizes the weighted signed logistic loss plus .01*a*a. Its derivative sums w*expit(eta+a) for label zero and -w*expit(-eta-a) for label one, plus .02*a. This stable derivative is mathematically an issued-error correction, but need not equal subtraction from rounded saved probabilities bit for bit. For nonzero a the new forecast is expit(original_eta+a). If a==0, retain the original saved probability after mandatory strict replay of expit(original_eta). This fixed identity branch preserves exact cold-start and balanced-history nesting despite permissible floating-point replay differences; it is not clipping or a solver fallback.

The objective is strictly convex, with derivative slope at least .02. Both solvers use the strict bracket plus/minus (sum(weights)+1)/.02. The producer uses Brent and the independent verifier uses bisection, both with xtol1e-12, rtol1e-14 and 200 iterations, successful convergence mandatory. The producer requires absolute full gradient at most1e-8 and verifier at most1.0001e-8. Empty history has canonical zero; exact computed g(0)==0 may return zero. No alternative solver or outcome-dependent tolerance repair is permitted.

All arithmetic is finite float64. Signed softplus and stable signed residual for finite logits must remain nonzero; reject underflow that loses a nonzero loss, weight, product or division. Forecast sigmoid endpoints are valid when rounded from finite logits. Compensated scalar sums, strict domain and zero-mask checks precede any comparison tolerance. The implementation and independent design specify full audit schemas and comparison tolerances before freezing.

## Prospective falsification

Register exactly two new Brier comparisons: calibrated versus frozen baseline, and calibrated versus the same frozen recent frequency. Both comparisons must lower mean Brier loss by at least .0005 in both original phases and have negative differences in both fixed evaluation slices. Require Holm2 p below .05/(19*20), and cumulative Holm131 p below .05. All129 previous hypotheses, including failed unevaluable attempts with p1, remain in the cumulative family. No newly registered baseline-versus-frequency contrast is permitted.

Inference retains HAC126 and centered circular block bootstrap lengths21/63/126 with199999 draws, new fixed seed20260925 and original phase/block offsets. Use the largest p across methods and phases. Nominal 5% HAC80% minimum detectable effects divided by .0005 are descriptive power diagnostics; they are not multiplicity-adjusted power or equivalence claims. Class support is unchanged: training1000 and50 per class; phase127 total and30 per class; slice15 per class.

A new scalar forecast is generated for each original scored origin. Two control forecasts are reused per origin, giving three combined rows. The original application count may exceed the scored count; every original application receives a calibration state. There are zero new monthly baseline fits. No empirical count is specified as an expected pass gate; counts must follow exact original cohort reconstruction and be independently verified.

## Registration, failure and interpretation

Prewrite synthetic timing, replay, numerical, tamper, cohort, inference, ledger and publication tests. Run the entire repository suite before freezing all code, protocol and designs. Hash-admit the complete old proof/publication closure and take single checked byte snapshots for decoded inputs. Register the two hypotheses before new calibration calculations, retain inherited records, and rehash the full manifest before completion.

An independent implementation reconstructs every issued logit, admission event, weighted record, scalar optimum, state, candidate probability, unchanged control, score, inference result and ledger event. It does not call the new producer's mathematical routines or refit the original baseline. Any source, arithmetic, support, solver, scoring, verification or publication failure preserves both new hypotheses as UNEVALUABLE with p1 and no lead; preserve computed but unverified evidence separately.

Publish interpretations only after independent verification and report/figure audit. A passing calibration would establish usefulness for this specified forecasting rule on reused archival history; it would not establish a new structural predictor, untouched confirmation, full conditional calibration or executable trading profit. Selection after earlier results remains adaptive despite causal timing and cumulative multiplicity accounting.
