# Peak-age prefreeze publication integration review

Status: PREFREEZE_INTEGRATION_REVIEWED, 2026-09-08. No unresolved issue remains in this bounded scope after the output-writing corrections below. Full repository validation, final freeze, registration, the historical run and independent numerical verification remain separate prerequisites.

This read-only review covers the implemented `peak_age_search` transaction, `peak_age_admission`, the complete `verify_peak_age` entry, `plot_peak_age`, their generated publication/admission/entry/plot contracts and retained RED/GREEN evidence. It extends the earlier mathematical `INTEGRATION_REVIEW.md`. The reviewer made no source or test edits, reran no mathematical tests, and accessed no real numerical source tables, new historical support, fits or scores. The only image inspected was the plot owner's generated six-effect fixture.

## Wiring, identity and inference

Both producer and independent literal CONTRACT objects equal the actual YAML, checked by parsing their AST literals without running research code. Source admission returns `(audit, daily, iv)`; production returns features, targets, feature states, scored panel, fits, full application states and support in the order consumed by the runner. The eight data outputs plus metrics and ledger exactly match the independent entry's ten checked byte snapshots. Tables are decoded from those snapshots. The helper receives the declared ten arguments and must verify 4n scored predictions and primitive losses, 4a full application predictions, a-n unscored applications, k schedules, 2k nuisance fits, k scalar fits and k training means. Scored and unscored cohorts are both retained.

Three registrations precede fallible source collection and numerical admission. A normal complete ledger is exactly 143 ordered records: 3 registered, 137 inherited and 3 evaluated. All three h=21 MSE controls and all six phase comparisons remain mandatory; the independent entry recomputes Holm3/140, both-phase effects, fixed later slices and all 126 phase/control/nonoverlap checks. A verification failure keeps every new hypothesis at p=1 and no lead. An intact regular 143-event ledger remains an exact byte prefix when three verification_failed events are added. A linked or unavailable ledger is not represented as intact registered history.

The manifest requires the complete current Python inventory, admitted closure, canonical inherited sources, freeze and prefit evidence, with every other prior protocol/report in preservation coverage. Entry verification checks exact inventory sets, frozen test evidence, the executing verifier, all ten outputs and protocol/manifest snapshots again before publication. Source admission shares checked traversal and raw snapshot decoding; this reuse is explicitly disclosed. It never invokes an old numerical admission/model pipeline.

The old wave20 failure/metrics/verification trio stays required, pinned and failed, including the exact original FAILED error record. It is reread after general digest passes. Successful index-hinge and waves18/19/21 failure markers, including dangling links, are checked on both sides of that final boundary; the current marker must also remain absent. The runner additionally refreshes the latest wave21 metrics/proof/publication pins. Existing attempt records or private output contents block a new run.

## Concrete correction and evidence

Review found that several current output leaves still used direct writes. An injected symlink could therefore redirect a report, table or failure-handler write into an existing artifact. The owners wrote generated regressions before correcting these paths. `OUTPUT_WRITES_RED.log` records four new runner methods with nine failed sentinel assertions. `entry_output_symlink_red.txt` records ten failed leaf/backup/success-proof cases; `entry_report_directory_symlink_red.txt` records two failed report-parent cases.

The runner now stages regular temporary files, flushes/fsyncs and replaces the destination for all five Parquet files, JSON, pre-run evidence, success/failure prose and interrupted-ledger backup. Canonical ledger writes already used replacement. The independent entry applies its own atomic writer to successful proof, failure records, prose, ledger and diagnostic backups. It rejects linked report-directory components and reads retained current leaves with O_NOFOLLOW, so arbitrary linked target bytes cannot become current metrics or ledger history. The reviewed regression cases preserve existing targets and do not create dangling-link targets. These are prefreeze publication corrections; no feature, model, gate, tolerance or empirical result motivated a change.

Retained post-fix evidence:

- `root_atomic_writes_green.txt`: 46 tests passed in 4.374 seconds: pipeline11, search9, publication26. `root_atomic_writes_lint.txt`: all checks passed.
- `entry_output_symlink_green.txt`: 25 entry tests passed in 5.489 seconds. `entry_output_symlink_lint.txt`: all checks passed.
- `PLOT_GREEN.log`: 11 generated plot tests passed in 0.315 seconds. Earlier `admission_entry_final_green.txt` separately records the 19 admission and original21 entry tests passing before the four entry regressions were added.

These are inspected owner execution logs, not claimed reruns by this reviewer. The separate unmocked mathematical helper coverage remains documented in VERIFICATION_DESIGN.md; entry wiring fixtures that stub mathematics are not substitutes for it.

## Figure and limits

The plot requires VERIFIED proof binding exact metrics and manifest bytes, preserved old failed-trio pins and absence of current/successful-ancestor failure markers. It rechecks these inputs after rendering and publishes PNG/PDF through temporary-file replacement. All three controls and both phases are visible. Coordinates use each phase's own control MSE; paired-loss interval [lo,hi] becomes [-100*hi/control_MSE,-100*lo/control_MSE], with positive improvement and a +0.25% effect gate. The generated PNG was visually inspected: six markers/intervals, labels, legend and limits are legible without clipping. No actual experiment figure was available or reviewed.

Sequential age correction can retune ridge shrinkage within the nuisance span. This review supports execution readiness within the stated scope, not conditional orthogonality, a historical predictive result, untouched confirmation or a feasible trading claim. Source-vintage/backcalculation limits and archival reuse remain unchanged.

Reviewed source SHA-256 identities:

| File | SHA-256 |
|---|---|
| src/peak_age_admission.py | 12d715e9b8ea8f72962f715d88c0263271629ecf2532abefc1675246c055b7e5 |
| src/peak_age_search.py | 69fd4792aabf7ea063f41875d07d13744be0719e8c7ceb19df5b55458cbcdfa2 |
| src/verify_peak_age.py | 8503bd4d5d536ed885f6950ca1bfddf4ccab9133d226612152b223eca77b3f22 |
| src/plot_peak_age.py | bb8789dcb5107be0bda8baa98834e3afe83b3e126882d8a4ba95ac7ce33c56ea |
