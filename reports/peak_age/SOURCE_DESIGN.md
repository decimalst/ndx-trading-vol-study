# Peak-age raw-source admission

Prospective wave22 source implementation, 2026-09-08. The source module constructs no features, labels, support screens, fits or scores. It preserves all completed prior code, protocols, outputs and failed records. New historical numerical admission waits for the caller's three registrations.

The two literal anchors are `event_cluster_replay.yaml` (SHA-256 `b3eeea877c536a25cde0edca69cd7cc1474a425a2458ac36978adfeacee7e76f`) and `reports/event_cluster_replay/publication_audit.json` (`1e962011d0ccf66776a05a09ba09e8420f5d48f4c646e6bc8616ec07c81bdac5`). Expected overrides used in generated fixtures must have exactly these two canonical paths and well-formed SHA-256 values; they cannot change the admitted source paths or source/proof schema.

`collect_input_pins(root=ROOT, expected=None)` returns the complete relative-path/SHA-256 mapping. It checks the wave21 publication, independent proof, freeze, code manifest, test-log identity, all report references, four verified outputs and six retained input identities. It recursively traverses all manifest code/input/preserved/existing-artifact groups using frozen checked-path/checked-byte primitives. The frozen wave20 replay metadata collector verifies the exact saved inner admission and all older transitive records. Its numerical admission loader is never called. Symlinks, path escapes, malformed or conflicting hashes, duplicate/nonfinite documentary JSON and incomplete anchor/output sets fail.

The wave21 proof must be VERIFIED with three new and 137 cumulative hypotheses. Its exact 140-event ledger must contain three registrations, 134 canonical inherited rows, and three terminal evaluated rows in that order. The ledger rows equal the checked canonical metrics; publication and proof bind the same output bytes. The old wave20 canonical failure, metrics and verification trio remains required and byte-exact. Wave18, wave19, wave21 and index-hinge successful failure markers must remain absent, including dangling symlinks. Every complete closure rehash ends with another required old failed-trio hash check, bracketed by successful-marker absence checks. This leaves the preceding failure as a failure despite the separately verified replay.

The source paths must equal the four paths in the pinned original `index_hinge.yaml`:

- `data/research_paths/spx_daily.parquet` — raw float64 SPX OHLC, source manifest identifying Yahoo Finance via yfinance, ticker `^GSPC`, `auto_adjust: false` and the exact data checksum.
- `data/free_sources/raw/cboe/VIX_History.csv` — DATE and CLOSE.
- `data/free_sources/raw/cboe/VIX9D_History.csv` — DATE and CLOSE.
- `data/free_sources/raw/cboe/VVIX_History.csv` — DATE and VVIX.

Each Cboe companion manifest must have literal version 1, the matching `cboe:daily-index:NAME` source identity, `daily-live` source version, exact checksum and byte count. The index-hinge manifest and VERIFIED proof must bind its own protocol and verifier bytes. Its fixed ceiling and protected start must remain 2025-10-20 and 2025-11-03.

`admit_upstream(root=ROOT, expected=None, registered_pins=None)` returns `(audit, daily, iv)`. If registration pins are supplied, they must include the entire checked closure with identical hashes. The whole closure is rehashed before raw decoding. All four raw byte buffers were captured and hash checked before any raw numerical parsing; parsers receive `BytesIO` snapshots, never a newly opened source path. SPX reads only open/high/low/close with a Parquet date predicate at the ceiling. Cboe reads date/value fields as text, parses dates first, filters to the ceiling, and only then converts retained values to float64. Neither post-ceiling Cboe numeric text nor old forecast tables are decoded. Another full closure and failure-boundary recheck follows decoding.

The bounded daily source must have a nonempty, unique, ordered, timezone-naive midnight DatetimeIndex in ms, us or ns representation. Its unit and stored float64 OHLC values are retained. Nonpositive/infinite observed prices and inconsistent complete OHLC ranges fail; genuine missing values remain on their original session rows. No complete-case filter changes the output reference calendar. Each bounded IV source follows the same date and observed-positive-value rules; joining IV series retains their union of observed dates without filling. The producer subsequently aligns them to the full daily reference calendar.

The returned audit has status `VERIFIED_WAVE21_BOUND_RAW_SPX_CBOE_SNAPSHOTS`, the complete `files`, `anchors`, `manifest_paths` and `manifest_groups` mappings, four fixed `source_paths`, previous output/input proof identities, and counts including 137 preceding hypotheses and 140 preceding ledger events. It records `source_end`, `sealed_start`, `loaded_from_checked_snapshots: true`, `numeric_post_cutoff_values_parsed: false`, `prior_forecast_tables_decoded: false`, `historical_models_refitted: false`, and the provenance/timing limitations. `sources` records bounded row counts, endpoints, missing-value counts and missing/outside-reference dates for all four sources. `reference_calendar` records row count, endpoints, input date unit and SHA-256 of its ASCII YYYY-MM-DD sequence, one session per newline-terminated line. Omitting an entire source date changes this calendar identity even if remaining observations have unchanged prices.

This route certifies byte identity and declared bounded timing, not original historical vintage or exact release latency. Prelaunch VIX9D training values remain archival back-calculations. The target remains a raw SPX price-index return without dividends, risk-free subtraction or an execution claim.

Nineteen prewritten generated tests pass. They cover documentary-only collection, full transitive preservation, exact proof/ledger identities, wrong anchors, checksum/schema/registration failures before numeric decoding, bounded checked snapshots, protected-tail invalid numeric text, missing-calendar preservation, finite positive float64 inputs, ms/us/ns transport, omitted-session identity, symlinks, and mutations at final source/failure boundaries. Initial missing-module RED and the additional late-marker RED remain saved; implementation corrections were made before any wave22 numerical admission. Scoped Ruff checks pass.

A permitted real metadata-only readiness check succeeded with 2,123 pinned files while both pandas raw numerical readers were mocked to fail on any call. It performed zero numerical-table decodes, fits or empirical scores. Its evidence is `admission_metadata_readiness.txt`. This readiness record is not a wave22 registration or predictive result.
