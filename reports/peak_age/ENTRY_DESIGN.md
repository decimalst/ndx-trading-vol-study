# Independent peak-age entry and inference

Prospective wave22 implementation, 2026-09-08. `src/verify_peak_age.py` contains the literal typed full protocol, independent inference and publication entry. It imports no new producer feature, fitting, scheduling or inference routine. The frozen independent index-hinge circular-bootstrap and Bartlett-HAC primitives remain unchanged. The new feature/model helper is `src/peak_age_verification.py` and reconstructs every saved input, fit, full application prediction and scored row independently.

## Source and publication identity

The entry first validates the entire current YAML against its typed literal, verifies the manifest's protocol signature and every registered code/input/preserved hash, and independently audits the admitted source envelope. Source traversal and raw checked-buffer admission are explicitly shared with `peak_age_admission`; the entry independently rehashes both anchors, the complete file set and every transitive manifest group/reference. It checks the completed wave21 VERIFIED proof and 137-family identity, the four declared source paths, and exact required failed-wave20 canonical trio. Old failure records remain failures and their values are never replaced by unpublished diagnostics.

The new manifest must cover exactly every current source/test Python file, the complete admitted source closure, canonical inherited metric sources, freeze and prefit evidence; its preservation group covers all other prior protocols and report files. The frozen full-suite log must have the expected passing count and hash. The focused pre-run log must record a positive passing test count and is separately snapshot-bound in the successful proof. The executing verifier's source bytes must match the frozen code inventory.

All ten new outputs are captured as immutable checked-path byte snapshots before decoding:

- `data/peak_age/upstream_admission.json`
- `data/peak_age/features.parquet`
- `data/peak_age/targets.parquet`
- `data/peak_age/feature_states.parquet`
- `data/peak_age/forecasts.parquet`
- `data/peak_age/fits.json`
- `data/peak_age/application_states.parquet`
- `data/peak_age/support_audit.json`
- `reports/peak_age/metrics.json`
- `reports/peak_age/trial_ledger.jsonl`

The saved upstream admission must equal the checked current admission. Table decoders consume snapshot buffers. The independent pipeline receives exactly ten positional arguments: daily, IV, protocol, features, targets, feature states, scored panel, fits, full application states, support. The entry independently counts n scored origins, a full application origins and k monthly schedules, and requires the helper to certify 4n predictions and losses, 4a full predictions, a-n unscored applications, 2k nuisance fits, k scalar fits and k training means.

After numerical reconstruction and inference, the entry rehashes all code/input/preserved/output bytes, rechecks the complete inventory and protocol/manifest/pre-run-log snapshots, and rehashes the whole source closure. It then rereads the exact required failed-wave20 failure/metrics/verification trio, bracketed by absence checks for successful wave18/wave19/wave21/index-hinge failure markers, including dangling symlinks. The new failure marker must also remain absent before the successful proof is written. The proof exposes `manifest_sha256` as well as the ten `verified_output_hashes`, allowing figures and summaries to bind their source pins to the independently checked manifest.

## Independent inference

The only candidate is peak_age and the ordered controls are baseline, depth and mean, all at h=21 with score `mse`. The independent scored-panel contract requires the exact declared schema, canonical origin/model ordering, four identical origin/target/timing/training cohorts, native known naive midnight dates, literal integer horizon/training counts, finite float64 targets/predictions/losses, and exact recomputed squared errors. Nonzero-error square underflow and nonfinite loss fail. The full calendar independently determines the previous-session feature cutoff and 21-session target end/availability; all rows must satisfy the phase fences and minimum 1,000 training observations.

Phase inference retains the frozen independent index-hinge arithmetic with the candidate name changed to peak_age: paired squared differences; relative and absolute MSE effects; two-sided centered-null circular bootstrap at blocks 126/252/504; Bartlett HAC504; interval envelope; annual diagnostics; both fixed evaluation slices; and all 21 nonoverlap offsets anchored to full-calendar positions. Every phase has at least 505 paired observations. The empirical protocol fixes 399,999 draws and seed 20260928, with horizon/phase/block offsets and shared draw designs across controls. The phase p-value is the maximum of HAC and all blocks; the hypothesis p-value is the maximum of both historical phases.

The entry independently reloads canonical inherited rows from their registered checked snapshots. All 137 remain, including the two civil-quarter failures, two civil-quarter-replay failures and three original event-cluster failures with each available conservative/wave/cumulative p-value exactly one. Unpublished scores never enter inheritance. It recomputes Holm3 against .05/(22*23) and Holm140 against .05. A comparison additionally needs at least .25% relative MSE improvement in both phases, negative paired effects in both later slices, and negative/nonempty effects for every offset in both phases. Only all three complete comparison passes yield leads `[21]`; individual improvements are not promoted. Diagnostic numerical comparison tolerances retain the frozen independent index-hinge conventions; identity fields and count types remain exact.

A normal complete ledger contains exactly 143 ordered events: three registrations, 137 inherited rows, three evaluated rows. Registration identities and every canonical row must agree exactly. A subsequent independent-verification failure appends three failure events, retaining the preceding bytes; its full ledger is then 146 events. Any failure writes all three canonical hypotheses as UNEVALUABLE p=1 with no lead, preserves unverified scored metrics as diagnostic-only, and leaves prior source/report outputs untouched.

## Generated evidence before historical admission

Twenty-five new entry tests pass. They exercise every protocol leaf mutation, independent bootstrap/HAC identity, exact equivalence to the frozen return-inference arithmetic after candidate rename, six phase comparisons and 126 offsets, the 505 boundary, strict new-wave significance, all-control gates, score/cohort/timing/count mutations, preceding failure preservation, checked source admission and all ten output/final-publication boundaries. Full-entry transport tests stub the numerical helper and inference to isolate entry wiring; the separate independent-helper suite tests its unmocked mathematical reconstruction and ms/us/ns transport. These scopes are distinct.

The initial missing-entry RED, full-entry RED, and prospective common-clock/manifest-binding RED logs are preserved. The latter identified that changing identical timing metadata across all arms left pure phase scores unchanged; explicit independent calendar and minimum-training checks now reject it. A generated YAML fixture was corrected to use actual YAML emission so small scientific-notation tolerances remain floats; the production literal validation correctly rejected its earlier string representation. No actual protocol tolerance changed.

A generated producer-evaluate/independent-verifier cross-check passed all three contrasts, six phases and 126 offsets with 19 synthetic bootstrap draws for test speed. Empirical invocation still requires the full literal 399,999-draw protocol. Its first generated calendar mistakenly included 2020-01-01 outside the declared evaluation start; that fixture was corrected, with the failed log retained. No historical datum or empirical score motivated these corrections.

An independent real metadata-only closure check succeeded over 2,123 pinned files and 23 manifests with both raw numerical readers mocked to reject any call. It performed zero actual raw numerical decodes, fits or new scores. Scoped Ruff checks pass. This establishes pre-admission execution readiness, not historical support, a predictive result, or conditional orthogonality.

## Final prefreeze output-link regression

The bounded final integration review identified direct report writes that could follow an injected current-output symlink into an old artifact after a verifier read rejected that link. Four additional generated tests reproduced ten leaf/proof/backup failures and two report-directory failures before the correction; both RED logs remain preserved. All successful-proof, canonical-failure, result, ledger and diagnostic-backup writes now stage new byte payloads in a checked report directory, flush and sync them, and atomically replace the current leaf. Existing leaf symlinks are replaced rather than followed. No-follow regular-file reads preserve only current regular metrics/ledger/backup bytes; a linked target is never treated as trusted trial history. A complete regular 143-event ledger stays an exact byte prefix of its 146-event failed record. Injected report or parent-directory symlinks are rejected before any mutation. Twenty-five focused entry tests pass in 5.489 seconds and scoped Ruff checks pass after this bounded correction. No protocol, feature, fit, score or inference arithmetic changed.
