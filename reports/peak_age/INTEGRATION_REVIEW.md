# Peak-age pre-admission mathematical integration review

This bounded read-only review covers the pure scheduling and evaluation parts of `src/peak_age_pipeline.py`, `src/peak_age_search.py`, their twenty prewritten tests, the current literal `peak_age.yaml`, the generated producer feature/model contracts and the independent mathematical helper. It does not cover the not-yet-reviewed run transaction, independent full publication entry, ledger recovery or final artifact publication. No historical data, feature/support calculation, fitted historical model or empirical result was inspected.

## Definitions and common cohorts

The literal protocol validates against the search module's exact typed contract. Programmatic comparison confirms identical RAW15, COMMON19, BASE17, DEPTH20 and four-model identities across the protocol and relevant independent/producer modules. The one21-session target,252-close window and251-interval return are retained. The stronger depth baseline includes three added nuisance slopes and is separately refitted; the unused original hinge is absent. The age correction freezes the current depth fit, centers raw age on the common mature training rows, keeps scale one and fits one scalar without another intercept or joint nuisance refit.

The pure pipeline's first common-feature-complete monthly application is selected before query-label filtering. Its exact earlier common-complete training rows require a finite target available by the preceding observed session and at least1,000 observations. All four arms receive identical training and complete application indices, including phase-boundary and final unscored origins. Targets are filtered for scoring only afterward. The independent helper reconstructs the same membership independently and verifies every fit's ordered training/application origin lists, full application-state predictions, scored subset and support counts.

Source missingness retains the full observed SPX calendar and literal rolling windows. Source ceiling, origin bounds, development target-availability ceiling and final target fence agree. A new feature requirement therefore changes the common cohort for every control; no frozen old forecast is reused as a newly fitted matched baseline. The full support dictionary agrees exactly: calendar/common-feature/monthly/application/scored/unscored counts, ordered unscored dates and each phase's n/first/last origin.

Generated integration initially exposed a concrete native date-unit introspection bug in the root pipeline: NumPy datetime dtypes do not expose `.unit`. The root's eleven prewritten scheduling tests recorded the failures in `pipeline_first_implementation.txt`; the owner replaced only unit introspection with `np.datetime_data(dtype)[0]`. The unchanged tests then passed in1.366 seconds. The independent helper's subsequent unmocked transported fixtures exercise this correction using original ms/us/ns source calendars, canonical producer output, Parquet/JSON serialization and complete model reconstruction. The timing/date policy itself was not altered.

## Mathematical checking and attribution

The independent implementation reconstructs the source controls and peak history without producer mathematical imports. New log ratios use a separate100-digit Decimal oracle; the producer uses the specified frexp/log1p arithmetic. Exact peak positions, ages, missingness and new-coordinate sign/zero semantics accompany fixed numerical comparison tolerances. Independent nuisance/scalar augmented least-squares optima challenge saved closed-form producer fits, including full gradients and objectives. The scalar verification conditions on the saved independently validated depth coefficients, preserving the actual sequential objective.

Both pipelines distinguish exact retained-table copies from solver tolerance: the independent helper requires scored labels to match the checked target values exactly and scored predictions to match their complete application states exactly, then independently checks primitive squared losses. Genuine zero coefficients preserve parent prediction bits; nonzero coefficients use the specified addition even for a centered-zero query. This avoids adding an undeclared signed-zero condition to the producer's arithmetic.

The protocol and report wording correctly acknowledge that raw centered age is not orthogonalized against nuisance columns. Because the nuisance fit is ridge-regularized, the sequential correction can partly undo residual shrinkage even when age belongs to the nuisance span. A generated independent test demonstrates this case. Consequently, an eventual benefit would be relative to the declared algorithms and controls; the model cannot by itself establish a new orthogonal information source.

## Inference and complete gate

The pure search retains exactly three contrasts, peak_age against baseline, depth and mean, for h=21. It preserves137 prior hypotheses and adds three for140. All three contrasts must pass. Every contrast requires >=.0025 relative MSE reduction in both phases, negative differences in both fixed evaluation slices, all21 nonempty/negative calendar-position offsets in both phases, at least505 observations per phase, wave Holm3 below `.05/(22*23)` and cumulative Holm140 below .05.

Paired inference reuses the existing mean-MSE comparison implementation with the unchanged Bartlett HAC504 and circular block widths126/252/504. The new seed20260928, horizon/phase/block offsets and399,999 draws are passed explicitly. Nonoverlap groups remain anchored to full source-calendar positions, before phase/missing-date filtering. Phase p-values remain the maximum across dependence checks and each hypothesis takes the maximum over both phases. The minimum Monte Carlo p1/400000 meets the prospectively stated resolution relative to the strictest raw wave cutoff1/30360. No favorable offset, slice, sign, horizon or single control can produce a lead.

The evaluator's counting matches independent reconstruction:4n newly generated scored rows,4a full application predictions, k schedules,2k nuisance ridge fits,k scalar fits and k training means. The pure failure record retains all three hypotheses at p=1 and no lead. The inherited helper validates pinned canonical input bytes when pins are supplied, preserves each source-row identity and rejects nonfinite/invalid p-values or an incomplete137-row family. Registered source closure and the complete runner/entry must supply those pinned inputs; this pure review does not certify unwritten publication paths.

## Evidence, scope and outcome

The root's final pure log records20 passing tests in2.387 seconds and scoped Ruff passes. Tests cover literal-contract mutations, actual previous/21-session clocks, common cohorts, query-label independence, unscored applications, exact model pairing, all3 controls, every statistical/effect/offset gate, incomplete support and complete failure accounting. Independent generated full-model transport tests provide separate numerical and application reconstruction coverage; the root's small scheduling fixtures deliberately mock fits, and their use is not represented as numerical model verification.

The reviewed pipeline function set is alignment, complete_mask, training_mask, validate_panel and build_panel. Its concatenated `ast.dump(..., include_attributes=False)` SHA256 is `c89edce900037314c95186a9da5f9f4d682bfc6893cbef8d258dad867a9398b8`. The reviewed search function set is validate, inherited, passes, candidate_leads, evaluate, failure_metrics and produce; its corresponding SHA256 is `63c2a70ae44252e693e9006fe3416468c9c558bad144d1356ba1679e042c2062`. These identify the reviewed mathematical bodies without implying review of a subsequently added run transaction.

No unresolved mathematical, cohort or gate mismatch was found in this scope after the date-unit correction. This is a pre-admission integration finding, not empirical evidence. Full run-transaction tests, the independent publication entry, full repository validation, freeze and registration remain separate prerequisites. Source-vintage caveats, protected-period exclusion, adaptive historical reuse and lack of an executable trading claim remain unchanged.
