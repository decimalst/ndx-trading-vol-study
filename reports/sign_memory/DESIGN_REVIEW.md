# Prefit independent review: strict directional agreement

The proposed family is a coherent new probability target on reused QQQ ETF/SPX price-index history. It does not supply a new information source, independent historical confirmation, or evidence about volatility magnitude. No new binary outcomes, event counts, predictor relationships, fits, or scores were inspected for this review.

The event is strict agreement: both raw intraday returns positive or both negative. A tie in either asset is a valid nonevent; missing or invalid paired returns remain unknown. Its complement must not be called only opposite signs. Direct sign comparisons avoid numerical underflow from multiplying tiny returns. Positive rescaling and simultaneous sign reversal preserve the event.

The 22-session agreement and marginal sign frequencies must share the same complete paired window on the full SPX reference calendar, ending one observed session before the forecast origin. No date compression, pairwise deletion, future session date, or next-open information may enter predictors. Labels mature at the next observed SPX close. Source/maturity fences, support requirements, and monthly fits must be fixed before counts are examined.

The empirical independence reference includes both positive and negative fractions, so exact zeros are handled consistently. Excess agreement is a bounded descriptive memory statistic, not an unbiased dependence estimator or a pure correlation parameter. Including marginal frequencies, their independence reference, continuous correlation and existing return/risk controls makes the incremental question concrete.

The baseline has 34 empirically scaled nonintercept columns: the original 33 plus the training-centered correlation square. The five added bounded controls use a fixed scale of one with training centering. Their algebraic dependencies are permitted because all slopes are penalized. An exactly constant bounded column is centered to exactly zero under a rule specified before inspection; its coefficient has a canonical zero optimum. The memory stage freezes the fitted baseline logit and adds only one centered, fixed-scale excess-agreement coefficient.

With normalized Bernoulli negative log likelihood and penalty .01 times squared slope norm, the baseline objective is strictly convex in finite parameters. Both-class training support prevents an intercept-only boundary optimum; slope regularization supplies curvature for collinear or zero bounded controls. The memory curvature is at least .02, giving a unique scalar optimum, including zero when memory is constant. These statements concern the declared objectives; numerical verification still requires finite arithmetic and the frozen gradient tolerance. The two stages are not a joint augmented fit.

Independent numerical checks should use a different optimizer: an analytic trust-region baseline solve from zero, and a bracketed derivative root for memory. A guaranteed memory bracket is ±(mean(abs(centered_memory))+1)/.02. Saved full-gradient checks and independent coefficients/predictions use tolerances fixed before fitting. Primary Brier scores should be reconstructed from saved-coefficient replay at the stricter replay tolerance, keeping independent optimizer tolerance from contaminating score inference. Evaluate NLL directly from stable logits; finite sigmoid saturation at probability zero or one is admissible for Brier scoring and is not repaired by clipping.

Both Brier comparisons remain registered even if support, arithmetic, optimization, admission, verification or publication fails. The proposed class-support gates are feasibility rules fixed before counts; nominal MDE remains descriptive, not a retrospective acceptance adjustment. The two added hypotheses raise the enumerated family from 117 to119, with all prior records preserved. A qualifying result would concern raw directional-agreement probability under this baseline; it would not establish covariance, volatility, direction-trading or profit predictability.

No conceptual blocker was found. The precise feature order, numeric source identities, solver budgets/tolerances, support thresholds, arithmetic policy and family accounting must be frozen in the new protocol before empirical construction. The original prospectus and all earlier code/protocol/report artifacts remain unchanged.

The completed prefit implementation uses independent trust-exact optimization from zero (target gradient 1e-10, at most 500 iterations) and a bracketed memory root (absolute tolerance 1e-12, relative tolerance 1e-14, at most 200 iterations). Acceptance requires the separately declared 1e-8 plus 1e-12 gradient bound, finite arithmetic and fixed coefficient/probability agreement. Every application is replayed, including wholly unscored months. Exact source snapshots and the complete previous VERIFIED record are checked without calling previous publication writers.

Peer review found and repaired protocol-guard omissions, unscored-month validation ordering, and score-admission guards for phase membership, label maturity and the training minimum before empirical construction. A synthetic calendar fixture initially admitted January 1, 2020 outside both registered windows; only that fixture was corrected. Actual probabilities must satisfy the strict [0,1] domain before tolerant replay comparison, and coherent near-endpoint tampering is explicitly rejected. Twenty-one owned synthetic contracts and six separate integration tests pass; initial failure records were retained before implementation. The successful ledger contains 117 inherited, two registered and two evaluated events, or 121 events for 119 hypotheses. Descriptive calibration reports only phase/model mean probability, observed frequency, their difference and Brier loss; it adds no bins, fitted calibration or gate.

Final entry-point review also caught the verifier initially looking for source and measurement audits in the private data folder. The temporary-tree test was first corrected to the runner's report-folder contract and reproduced the failure; the verifier was then corrected before any empirical run. Both audits are now snapshot-checked under `reports/sign_memory`, while admission, features, targets, forecasts and fits remain under `data/sign_memory`. The same 27 synthetic checks pass after this repair.
