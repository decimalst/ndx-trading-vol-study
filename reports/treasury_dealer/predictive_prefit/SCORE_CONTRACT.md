# Prospective Treasury daily and activation-date score contract

No new historical arrays, fits, support or scores have been read or computed. All implementation tests use invented panels. Frozen treasury_dealer.yaml is authoritative; the public evaluator rejects changes to the fixed numerical/date/support/family contract. Smaller generated arithmetic exercises use private helpers or mocked inference, never relaxed public defaults.

API evaluate(panel,calendar,activation_mask,prior,protocol,support_report) returns COMPLETED metrics only after full preflight. activation_mask is an exact bool Series aligned to the entire unchanged naive-midnight reference calendar; its upstream meaning is auction_count>0, including zero/cancelling surprise pulses. The independent forecast/support reconstruction owns authenticating that meaning against features and source events. Scoring never infers activity from a nonzero candidate forecast or observed difference.

Both contrasts are candidate minus matched/market QLIKE at horizon5. All3arms must share exact origins, targets, endpoint dates, offset, phase and other metadata. QLIKE is independently recomputed from positive finite y/prediction; every endpoint is the fifth following full-calendar position and offsets are global original-calendar position modulo5. Dates stay bounded by2025-10-20 and development labels end by2019-12-31.

Inference domain: for each phase, retain every original reference-calendar position INSIDE that phase's declared bounds, including interior missing/nonauction dates and immature phase-end dates. Do not pad with out-of-phase history and do not compress to scored or active dates. Reindex paired differences to that phase calendar. Daily mask selects scored origins; active mask selects scored origins AND the supplied activation flag. Missing differences remain unselected. Each endpoint uses frozen treasury_dealer_inference.masked_mean_inference: full-calendar circular paired(mask,mask*difference) block ratios21/63/126,399999draws,zero-support replicate fatal, full-calendar influence HAC126. Seed is20261001+phase_code*10000+endpoint_code*1000000 with development/evaluation0/1 and daily/active0/1; the frozen primitive adds block internally. The two contrasts share the same phase/endpoint seeds. full_calendar_n counts every phase position.

The support report has exact top keys status,passed,monthly,phases,slices,offsets,discrepancies and must be SUPPORT_PASS/True with empty discrepancies. Scorer independently matches scored daily and active counts for both phases, two literal evaluation slices and all five global offsets. Event counts must equal the sum of six per-tenor integer counts; activation dates cannot exceed event count or daily count. Enforce phase505daily/104active/12events each tenor, slice252/52/6, offsets63/20 (no offset tenor floor). Every scheduled month enforces1000training rows,200training activation dates,24events each tenor. Check monthly identities and training count/cutoff against scored rows and application count range; exact unscored applications and source event/tenor memberships belong to independent forecast/support verification. No month, event type or offset is dropped to recover support.

Result top keys: status,rows,inherited_rows,leads,hypothesis_count2,cumulative_hypothesis_count146,common_scored_origins,common_active_origins,support_report. Row keys: study='treasury_dealer',candidate='candidate',control,horizon5,score='qlike',phases,p_conservative,p_holm_wave,p_holm_cumulative,verdict. Each phase has name,daily,active,p_conservative,offsets,stability,annual. Each endpoint keeps the exact frozen inference fields mean,n,full_calendar_n,hac,block_inference,p_conservative plus ci95_envelope,candidate_loss,control_loss,first_origin,last_origin. Diagnostics record daily_n,active_n,daily_delta,active_delta plus offset, slice bounds or year/calendar counts. Means with zero annual support are None; every required phase/slice/offset has positive supported counts.

Comparison p is maximum across both endpoints, both phases, allblocks/HAC. Holm2 must be below.05/(25*26), cumulativeHolm146 below.05 after appending both new p-values to all144 exact inherited records. Daily difference<=-.005 in both phases, active difference<0 in both, and daily+active differences<0 at all required evaluation slices and all offsets in both phases. Both contrasts must pass to emit the sole lead treasury_dealer. Fitted score failures abort the entire attempt; failure_metrics(error,signature,prior) retains both rows as unevaluable with allp=1 and all inherited rows intact, terminal status UNEVALUABLE, error_kind UNSUPPORTED for INSUFFICIENT_DATA otherwise FAILED. A two-argument failure_metrics(error,signature) call cannot recover prior rows; the runner must attach the authenticated family before publication. Supplying the optional third prior argument preserves it directly. The family helper _prior defaults to144; _probabilities and inherit_family are also exposed. Prior comparison source identities/hashes and failed records remain unchanged.

Independent verification does not call producer evaluate or its scoring helpers. verify_scores(panel,calendar,activation_mask,metrics,prior,protocol,support_report) reconstructs the entire metrics object, then returns VERIFIED with comparisons_verified2, endpoints_verified8, bootstrap_runs_verified24, hac_runs_verified8, inherited_comparisons_verified144 and the common daily/active counts. The verifier uses separate explicit circular-index block tables instead of cumulative-prefix sums, independently authored full-calendar influence/HAC and mean/gate calculations, and frozen generic independent Holm/comparison utilities. It generates all complete-block starts before any tail starts, preserving the fixed RNG contract. HAC uses independently assembled lag products with the same float64 dot-product reduction to preserve exact probability arithmetic.

Shared dependencies are treasury_dealer_protocol.validate for immutable metadata, verify_claims_release_scores._compare/_holm/_integer/_real for generic independent comparison/arithmetic contracts, and verify_commodity_implied_scores._panel for independent scored-panel/date/QLIKE validation. Only a copied column label is adapted from treasury_cutoff_date to commodity_cutoff_date for that validator; a separate check enforces the stronger Treasury2010 source floor. No source data or producer forecasts are reconstructed using producer routines. Probabilities, counts, dates, schemas and inherited records are exact; other score numerics use the existing independent comparator's absolute1e-10/relative1e-8 tolerances. Event identities, source-clock authenticity and unscored applications require the separate forecast/support verifier and runner pins; count agreement alone cannot authenticate them. This is computational independence, not a claim of a separate human author. No calibration or market-coverage guarantee follows from an agreement check.

Prewritten evidence: SCORE_RED.log and SCORE_VERIFICATION_RED.log capture actual missing-module failures before the respective implementations. SCORE_FIRST_IMPLEMENTATION.log records9 generated producer tests passing; SCORE_VERIFICATION_FIRST_IMPLEMENTATION.log records6 generated independent tests passing. Cases cover fixed support and protocol rejection before resampling, paired QLIKE/target/global-offset corruption, full phase gaps/tails, shared seeds, inactive versus true-zero active effects, worst-method and single-control vetoes, retained failed families, explicit circular-index/literal bootstrap and HAC oracles, fatal zero-support replicates, and corruption of every reported inference/family/gate/diagnostic group. Generated end-to-end inference tests privately reduce draw counts with explicit mocks; the public protocol retains399999. Cosmetic import ordering and immediate-use closure bindings were then corrected for lint without changing arithmetic. Final generated checks and scoped lint are retained in SCORE_GREEN.log and SCORE_VERIFICATION_GREEN.log. No historical feature, support, forecast or score computation is part of these tests.

Prefreeze publication integration amendment: unqualified generated core metrics remain valid. If either source_clock_class or source_clock_limitation appears, verify_scores requires both exact strings defined by the shared metadata constants CLOCK_CLASS/CLOCK_LIMITATION in treasury_dealer_protocol. The verifier never imports the runner. The class is QUALIFIED_REPORTED_CLOCK_ONLY_FOR_Z52; the limitation preserves the qualified cached Treasury-attributed January27,2020 release-date assumption, absent original PDF/public-delivery timestamp and quantities, strict-clock consequence of rejecting that assumption, and prior regression access to the later period. These fields are verified on the actual attached metrics object without mutating it, so the runner can qualify results before verification. SCORE_QUALIFICATION_RED.log records the prewritten positive case's original schema rejection; missing, altered and mistyped qualification variants are retained. SCORE_QUALIFICATION_GREEN.log covers all8 independent tests plus scoped lint and formatting. No statistical arithmetic, threshold, protocol YAML or producer scoring code changes in this amendment.
