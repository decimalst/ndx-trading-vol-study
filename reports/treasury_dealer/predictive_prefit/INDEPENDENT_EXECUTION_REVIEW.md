# Independent Treasury execution review

Review date: September 9, 2026. Disposition: **no remaining blocking execution issue found in this bounded pre-empirical review**. This review does not register a comparison or authorize a result verdict by itself. The next execution must still pass its selected-test gate, create the prescribed freeze, and register the 144 inherited plus two new comparisons before source decoding.

I read the new input, protocol, search and test-selection implementations and their generated tests, the execution contract, the preserved regression evidence, and the final score-qualification interface change. I did not run the repository suite, decode historical market values, construct an empirical cohort, fit a model, or calculate predictive results. My separate support implementation is producer work; the source/support reconstruction is independently implemented by another reviewer. This execution review does not substitute for that reconstruction.

## Admission and ordering

The literal YAML hash is checked before parsed configuration is accepted. Exact configuration comparison preserves types and rejects altered floors, family counts, dates, features and extra keys. The execution module binds the four inherited market snapshots, qualified source ledger and source evidence, unchanged calibration, prior result family and new output directories. Existing source admission reconstructs the prior 144 identities; the runner copies them before the callback can mutate its arguments.

The durable trial journal contains two ordered registrations and all 144 inherited rows before the numerical callback. The input wrapper checks all five supplied byte payload hashes before ledger decoding, then calls the frozen market reader, which rechecks its own buffers and validates date metadata before bounded numerical extraction through October 20, 2025. The join retains the complete original reference calendar and missing positions. This call ordering is covered by the generated actual-runner test, with source loaders mocked and a deliberately failed support report.

Support is computed and independently reconstructed before any monthly fit. A support failure stops the entire registered attempt; the generated integration asserts that neither fitting nor scoring is called. Successful support proceeds to fixed common-cohort monthly fitting, independent forecast reconstruction, scoring and independent inference verification. Signatures and keyword arguments agree with the final helper APIs. Both exact source-clock qualification fields are attached before score verification; the verifier accepts the original unqualified numerical result or the exact complete qualified pair, and rejects a missing, altered or wrongly typed qualifier. Its arithmetic is unchanged.

## Review findings and resolved failures

The initial wrapper allowed a final pin failure or publication error to escape after completed metrics had been written. The revised executor performs a final check before and after provisional publication inside its failure handler. It preserves provisional metrics as an attempted artifact, publishes canonical failure metrics with both probabilities equal to one, retains the inherited family, and appends invalidation records. A terminal-publication exception uses the same mechanism and preserves any partial terminal. Existing attempts are not silently restarted. A provisional verification file or attempted metrics file alone is therefore not a completed publication: the canonical metrics, failure state, journal and terminal must be read together.

The initial freeze verifier accepted empty inventory maps. It now requires the complete current source/test Python inventory, all mandatory fixed input pins, and the four saved test-evidence files. It checks hashes, positive consistent executed counts, the exact six quarantine identities, selected identity uniqueness, absence of ordinary skips/failures/errors, and the selection/log hash bindings. The selected suite records every selected and quarantined identity before executing cases. Its generated selection fixture is a plain callable and cannot be accidentally discovered as a real test case.

Observed regression evidence was preserved before each relevant fix. `EXECUTION_REVIEW_REGRESSIONS_RED.log` records the late-check/publication/qualification failures; `FREEZE_INVENTORY_RED.log` records acceptance of an empty forged freeze. The subsequent search/selection suite passed 15 generated tests in 0.085 seconds, and the final execution/protocol/selection check passed 20 in 0.190 seconds. `SCORE_QUALIFICATION_RED.log` records rejection of the qualified metrics schema before the interface fix; its final check passed eight generated verifier tests in 5.547 seconds, with Ruff and format checks clean. These are inspected saved outcomes, not a new test execution by this reviewer. The source preflight separately records 29,875 verified pins and 144 inherited comparisons; I read that saved metadata but did not repeat its complete hash traversal.

## Historical-access correction and limits

I independently confirmed the six access paths in `tests/test_methodology.py`, the configured November 3, 2025 clean-period start, and their successful outcomes in the earlier commodity `FULL_PRECHECK.log` at lines 1225–1233. Those tests load later historical observations, refit HAR models, or invoke evaluation on that period. The previous blanket claim that those numerical observations remained unopened is false for regression-suite execution. The additive correction preserves the old files and results and expressly withdraws an untouched-confirmation claim.

Quarantining those six cases before this Treasury execution is a data-access restriction fixed before empirical inspection, not a response to their results. All other discovered tests remain selected; the forthcoming outcome must be called a selected-suite result with six disclosed quarantines. Neither this change nor the bounded current decoder makes the historically evaluated period untouched again.

Every canonical completed/failure metric and terminal carries `QUALIFIED_REPORTED_CLOCK_ONLY_FOR_Z52` and its exact limitation: January 27, 2020 Z52 uses a qualified cached reported-date assumption; the original PDF and historical delivery timestamp are unavailable, and quantities remain unknown. Rejecting that assumption leaves the documented unbounded strict-clock uncertainty. The saved synthetic calibration is diagnostic evidence at unchanged settings, not a guarantee of market interval coverage. No predictive qualification, empirical sample adequacy or untouched-future confirmation is established by this review.

## Reviewed byte identities

The following hashes identify the reviewed versions and inspected evidence; no frozen source or report was changed by this review.

| Path | SHA256 |
| --- | --- |
| `treasury_dealer.yaml` | `194fd2ef49cfa8db5542f5e672383b5e2d4ec8f8c724b2af8559d51319aef6a8` |
| `src/treasury_dealer_inputs.py` | `ebd867c3c0a27386f706269f73e1370be488d37139239e083da99a038b210a61` |
| `src/treasury_dealer_protocol.py` | `05dfa4398328454ee5282a1e28ba50e5bf3837325a22dfccecb35767c5b4e0c6` |
| `src/treasury_dealer_search.py` | `287e461439de04fb6406a3b4ec63e457c9d761bd144292c8a7511a5f496b805b` |
| `src/treasury_dealer_test_gate.py` | `e026053f6223b7db8d6026adf111f73f4fd7c3e11f48d0c6d5951c9db238560a` |
| `src/verify_treasury_dealer_scores.py` | `e6cf516b193bc42e51573aa584ef36912c54af96ccba2359a941c3625b7c0615` |
| `tests/test_treasury_dealer_inputs.py` | `5af5e2c8731a99d0a2454ba202960bf420f7f920ce61ec87ff3d6b7749ffa988` |
| `tests/test_treasury_dealer_protocol.py` | `0ee2a47fe2799d8cf966a66d82e0b9bff52df0292ad2cba44ef67f506ea2623e` |
| `tests/test_treasury_dealer_search.py` | `4035755aa22baf968dae8a48c677fd1a594e01ae5bfd3514a0f2463d2e9029d5` |
| `tests/test_treasury_dealer_test_gate.py` | `5f109106872d9f70c50cf7421dd9c5b5bd786d8a935a85f4969427abdc128da6` |
| `tests/test_verify_treasury_dealer_scores.py` | `27182682321245a0bf6bd9a5112fe211c1c70a44121c78c23f48441b67435f95` |
| `reports/treasury_dealer/predictive_prefit/EXECUTION_CONTRACT.md` | `059d0b2d6205c841b8f00da74cd93964c4489fe0c62ffb32b1be75f026183a75` |
| `reports/treasury_dealer/predictive_prefit/HISTORICAL_TEST_ACCESS_CORRECTION.md` | `325915e178d0ebc58ac05ced3d281a8c8ec840ed86fd9fec348e5ae0969ab007` |
| `reports/treasury_dealer/predictive_prefit/EXECUTION_REVIEW_REGRESSIONS_RED.log` | `3ce51cdaf890d5613fda5b0ed62cd4984bb815ead0832a042945839d4c6ddca5` |
| `reports/treasury_dealer/predictive_prefit/FREEZE_INVENTORY_RED.log` | `ef441128f75ae8acf73e299b2fb82877ba7999bc3e26551349272001b7487edd` |
| `reports/treasury_dealer/predictive_prefit/SEARCH_REVIEW_FINAL_GREEN.log` | `d9c95c91897472f9db3c5392472b5bab07411edab060afee286982647f1fc0f0` |
| `reports/treasury_dealer/predictive_prefit/EXECUTION_STABLE_GREEN.log` | `e9efe262c8bc4697b287a7803621d5ffb563fea5ec7f6bb45846e419c0f92eea` |
| `reports/treasury_dealer/predictive_prefit/SCORE_QUALIFICATION_RED.log` | `ae20ee401a041f04eb10d18828600f793393672e9297f6779c28dc6c31a170da` |
| `reports/treasury_dealer/predictive_prefit/SCORE_QUALIFICATION_GREEN.log` | `43234ebdec4e1a26fff476cdd8c4fc8e8a0bfd4cc0e63ab50dfe7db6fd51a219` |
| `reports/treasury_dealer/predictive_prefit/SOURCE_PIN_PREFLIGHT.json` | `fc55149e10dd7eb9607dff972a8b368f275b920b1e0f1cce04852d74d1f27a09` |
| `tests/test_methodology.py` | `3a2da7e19b62b063dd8d73ef0f892b58e35ee6b3a099a028c547510a705e7c96` |
| `config.yaml` | `b835935765b2a5b67321d47b93380373518e90ce6d31c0a210452e90e24cc7e4` |
| `reports/commodity_implied/predictive/FULL_PRECHECK.log` | `b6037a22de43d5df35ed2b5eb553dddc1e5c7d5a7faf4d2c46f70bb2ae787374` |
