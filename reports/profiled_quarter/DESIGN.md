# Independently profiled verification of the civil quarter-end risk experiment

This seventeenth attempt repeats the same SPX forecasting question after two different failures. Wave 15 failed source staging before producing forecasts. Wave 16 corrected the source package and generated forecasts, but its independent optimizer failed the frozen stationarity requirement. Both attempts remain unevaluable with two p=1 comparisons each. The wave-16 training-only diagnosis identified a numerical stopping problem; its unverified predictive scores are not used to select a model or a threshold.

The new work changes only the independent baseline optimization. The producer still uses the exact frozen feature, model and proper-score functions. The independently reconstructed baseline analytically eliminates the unpenalized intercept and solves the resulting slope problem. Its weighted covariance Hessian plus .02I proves uniqueness in exact arithmetic. Its accepted coefficients must still satisfy the original full-gradient, coefficient and normalized-prediction comparisons. The independent scalar quarter correction remains the same bracketed root problem. A successful solver status or a small objective change does not override these checks.

The numerical method was specified and challenged on synthetic inputs before registration. The previous prospectus described a conservative analytic derivative bracket whose remote endpoint can underflow even when the optimum is representable. A generated counterexample exposed that issue. The final [solver specification](/Users/byrons/code/trading-vol/ndx-vol-experiment/reports/profiled_quarter/SOLVER_DESIGN.md) starts with the Newton-step distance, expands finite negative-derivative trials, and bisects below invalid upper barriers within a fixed 60-evaluation budget. Brent's derivative root solve then operates only on a finite bracket. All accepted states must retain the original domain, and an unresolvable barrier fails explicitly. No historical training matrix, candidate score, application prediction or previous failed optimizer state selected numerical settings for this new implementation.

## Preserved scientific question

The candidate is the last-five-civil-date indicator for March, June and September. It adds one centered penalized slope to a frozen fitted baseline. December is already controlled by the year-end indicator. The 31-column baseline retains the original eighteen market/macro columns, eleven month indicators, ordinary month-end and year-end. The other control is the same-sample arithmetic training mean. No extra target, month window, interaction, subgroup or model arm is introduced.

The target remains next-session SPX max(Garman–Klass,1e-10) plus squared raw overnight log return. Its units are squared log returns. It is a daily OHLC risk proxy, not measured high-frequency integrated variance. Predictors use the exact old calendar, publication rules, rolling histories and missing-source masks. The nominal calendar skips weekends only. The source fence is 2025-10-20; observations from 2025-11-03 onward remain sealed.

All three models use the same complete predictor cohort and mature labels. Monthly fitting starts at the first feature-complete application, before filtering query outcomes; wholly unscored months are retained. Each training group and each declared evaluation slice must pass the unchanged support, civil rank and conditional nonredundancy gates before any optimization. The expanding training history, one-session lag, 1,000-row minimum, mixed feature geometry, target normalization, .01 slope penalties and proper-score arithmetic are unchanged.

Development remains 2016-01-04 through 2019-12-31 with labels available by the latter date. Evaluation remains 2020-01-02 through 2025-10-17; the two fixed late slices are 2020–2022 and 2023–2025. A qualifying quarter-end increment must reduce mean proper loss by at least .005 against both controls in both phases, improve in both late slices, pass wave Holm2 at .05/(17*18), and pass cumulative Holm127 at .05. HAC126, block lengths 21/63/126 and 99,999 bootstrap draws remain fixed; the new recorded seed is 20260923. These are exploratory gates on reused history, not untouched confirmation or a trading-return test.

## Source and failed-attempt preservation

The successful source anchors remain the original calendar-variance and causal-pooling studies. Complete independently reconstructed proof records are required, using the already specified source-document closure. The old source parser and documentary exclusions remain unchanged. Captured documents and archival market values do not establish stronger historical vintage quality than previously documented.

Wave 15's frozen protocol, manifest, empty output directory, two p=1 rows and terminal ledger remain required. Wave 16's frozen protocol, manifest, publication audit, failure and verification records, generated-output hashes and terminal ledger must also match. Its earlier evaluated ledger entries are preserved diagnostics; its canonical failure rows supply the inherited p-values. Its 6,297 generated forecasts and 101 fits are not inputs to the new predictive evaluation. No failed verifier or old writer is rerun for admission.

The new family inherits 125 comparisons and registers two more, totaling 127. Both new comparisons remain p=1 if source admission, support, fitting, scoring, verification or publication fails. No fold, source, arm or failed attempt can be deleted in response to an outcome. Code, protocols, results and diagnostic artifacts from previous attempts remain unchanged.

## Required checks before interpreting a result

Prewritten tests cover profiling identities, derivatives and high-precision oracles; loss changes below floating-point resolution; intercept and penalty errors; extreme arithmetic; bounded line solving; false convergence; full synthetic source staging; strict historical maturity; every model and forecast; failed-attempt preservation; and whole-family failure publication. The complete repository suite and the focused entry checks must pass before registration. Exact solver constants and a separate design review must be frozen with all new code before the historical replay.

After the producer finishes, a separate verifier must reconstruct every fit, application prediction and score comparison, both source proof records, both prior failure records, and the complete hypothesis ledger. Scores remain unaccepted until that verification succeeds. Any result must preserve the archival, nominal-calendar, measurement, multiplicity and reused-history limitations.
