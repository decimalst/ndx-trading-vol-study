# Prospective execution contract

Previous goal turn: **progress**. The Treasury study completed and its additive publication checkpoint was saved;146 comparisons are now recorded. This is a new model experiment, wave26 with three comparisons, not a continuation or modification of the Treasury estimates.

Before any empirical decoding, support inspection or model fitting, preserve the exact new protocol, every then-current Python source/test, previous root protocols/reports and inherited input hashes. Run all discovered repository tests using the existing exact six historical-replay quarantines. The exclusions and the prior historical-access correction remain explicit. Save code hashes before and after the tests and reject drift. Require a completed independent prefit review.

Durably record all three new comparisons and the146 inherited full dictionaries before the numerical callback. The original full-calendar market reader and feature/target definitions are reused. No Treasury source quantities or qualified Z52 clock enter this model experiment. Keep private derived output files under the already ignored data/model_memory_study/precision_gate_wave26 directory, directory mode700 and files600.

Retain all feature-complete issued forecasts, model memberships and audits, including expert warmup months, gate cold starts and unscored applications. Independently reconstruct features, targets, expert fits, gate membership, optimum/stationarity and all forecasts; a saved-only score check cannot substitute for this. Then score the three fixed comparisons and independently reconstruct the full-calendar paired inference. Before and after provisional publication, recheck the frozen input/code/report/test hashes. If numerical work, verification, final preservation or publication fails, preserve any provisional output additively and record all three comparisons as unevaluable with p1. A registered attempt is never silently restarted.

Independent numerical tolerances are fixed before empirical inspection: expert coefficient abs/rel1e-7; direct forecast abs1e-12/rel1e-7; other arithmetic abs1e-12/rel1e-9. The independent gate optimizer uses SLSQP, start.5, analytic gradient,ftol1e-14,maxiter2000; require success and projected gradient<=1e-7. Compare its coefficients to the producer at abs5e-6/rel1e-7 and objectives at abs1e-10/rel1e-9. Separately reconstruct the saved coefficients' objective, gradients, projected stationarity<=1e-8 and direct predictions. Iteration counts are range checked, not claimed to reproduce across different solvers. Score numeric abs1e-10/rel1e-8; identities, support counts and probabilities exact.

The fixed issuance request begins2010-01-04. Before2016, expert months with fewer than1000 mature rows produce explicit warmup and no forecast; at/after2016 that deficiency aborts. The gate's252-pair startup uses baseline-valued forecasts and remains scored. These rules were declared before historical support access. A later failure does not authorize shifting the start or deleting cold-start dates.

Independent prefit review identified two binding gaps before empirical access. The execution now hashes the exact freeze-record bytes read before decoding and rechecks that original signature at every final boundary. Each output is hashed immediately after its first save; the complete seven-file inventory is checked before numerical verification and again before its certificate, before/after metrics publication, and before/after terminal publication. A changed freeze file or saved output invalidates the complete attempt and preserves provisional evidence. Prewritten mutation regressions have explicit missing-helper RED records followed by passing checks. Earlier frozen runners are unchanged; this is a correction to the new execution path before registration.

A separate review found the same issue for saved reports. Metrics, manifest, verification certificates and terminal now bind their exact intended JSON serialization; the journal binds its original payload and each append against the previously verified prefix. All bound report hashes are checked at the same final boundaries. The prewritten post-publication metrics-mutation test first reproduced an incorrect COMPLETED status, then passed after this correction. The three fixes change preservation enforcement, not the model specification, scoring criteria or an observed empirical result.
