# Prospective replay with complete source dependencies

This is an unregistered next-attempt design after wave 15 stopped during admission. Wave 15 created no new civil features, support counts, models, forecasts or scores. Its two failed comparisons remain p=1 in the cumulative 123-hypothesis family. Its frozen source/test files, protocol, manifest and terminal failure artifacts are not repaired or replaced.

The [bounded dependency audit](/Users/byrons/code/trading-vol/ndx-vol-experiment/reports/civil_quarter/SOURCE_DEPENDENCY_AUDIT.md) identifies four omitted runtime extraction snapshots and one documentary capture manifest, all with matching inherited hash bindings. The next attempt should first build a complete, explicit source dependency inventory from the bounded macro-plan ledgers and annual-plan coverage metadata. Each referenced document must have an unambiguous repository-relative path and a declared cryptographic hash inherited through an already pinned metadata file. Resolve and validate these references before numeric feature extraction. Check conflicting references, missing files, path escape, malformed hashes, same-byte decoding, and out-of-publication-fence exclusion. Register all admitted document hashes explicitly. File presence alone is not admission, and a document lacking a bound declared hash cannot be added by convenience.

The documentary corpus also retains an explicitly superseded intermediate amendment that names an older hash for the current payroll-ledger pathname. Keep that historical metadata unchanged and identified as provenance. It is not an active source binding or an alternate hash to accept. The new active closure must follow the selected final ledgers, their current artifact maps and the frozen source-reader rules; do not collapse all historical mentions of a pathname into a permissive multi-hash map.

Use that inventory to construct the immutable temporary source tree needed by the unchanged older source verifier. Preserve the original publication fences, source eligibility and original-plan parsing rules, unknown/reissued-document exclusions, daily risk measurement and every old audit comparison. Normalize only documentary source_path prefixes when comparing staged and original audits. The previous implementation failed because the direct manifest inventory was not the complete set of document dependencies followed by the old scanner. This packaging change addresses that omission; it does not create a new historical vintage or improve rights, release-latency or exchange-calendar evidence.

Independently reproduce the full old calendar-variance and causal-pooling successful proof records using the complete source package. Separately admit wave 15's failed registration: literal failed protocol and frozen hashes, the two canonical p=1 rows, both terminal ledger events, and the absence of new output artifacts. Never interpret the failed attempt as a successful no-signal experiment or discard it from inherited inference.

If all dependency gates can be met, register a new wave 16 using the exact same wave-15 civil features, 31-column baseline, single staged quarter slope, historical-mean control, training geometry, penalties, optimizers, support/rank/identification gates, maturity rules and scoring arithmetic. The model question has not yet received a historical result. Reuse the frozen mathematical implementations with explicit, tested interfaces rather than silently mutating their module globals. No new civil window, target, subgroup, fitting fallback or forecast correction is proposed.

Both repeated comparisons count as new registrations, producing **125 cumulative hypotheses**. A prospective wave allocation is Holm2 at **.05/(16*17)** and cumulative Holm125 at .05, with the original **.005** absolute proper-score decrease required against both controls in both phases and negative deltas in both fixed late slices. Use the same HAC126 and block widths 21/63/126, 99,999 draws, and prospective seed **20260922** plus phase_code*10000+block. These are proposed bookkeeping changes for a new attempt, not edits to the failed wave-15 contract.

Before registration, write synthetic tests that place a bounded valid macro document outside the original direct-manifest directories; require explicit closure staging to make the frozen scanner succeed. Tamper with the document after its metadata is pinned and require failure before decoding. Also test duplicate/conflicting path bindings, out-of-fence entries, missing declared hashes, source-month unknowns, unchanged old proof identities and both inherited p=1 rows. Use a separate implementation to reconstruct the dependency inventory, models, predictions, scores and cumulative accounting. Then run the complete repository suite and focused checks and freeze the new attempt before empirical civil construction.

No source acquisition, new economic mechanism or alternative model is included in this replay proposal. All existing archival, original-plan, nominal-window, daily-risk-proxy and reused-history limitations persist. A failure to establish complete source admission must stop the new attempt rather than be hidden by source deletion or a looser gate.
