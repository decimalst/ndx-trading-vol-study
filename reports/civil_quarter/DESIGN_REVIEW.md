# Pre-run independent design and implementation review

All civil features, models, scores, inference and publication code are new wave-15 files. Earlier frozen Python files, source contracts, protocols, reports and raw artifacts remain unchanged. The root separately checks the full prior manifest chain before registration. The design follows the prospectus written before any new empirical civil feature, support count, fit or score.

A modeling reviewer implemented the mixed geometry, optimizer-free support/rank/identification audit, deterministic baseline Newton fit, frozen-baseline scalar increment and complete monthly application accounting after prewriting synthetic tests. A second reviewer implemented independent civil arithmetic, trust-exact baseline optimization, bracketed scalar root finding, saved-coefficient and normalized prediction replay, complete source admissions, bootstrap/HAC inference and terminal publication-failure handling. A third reviewer implemented feature/calendar contracts and a separately tested scientific plot, then joined the actual producer and independent verifier on a long generated calendar. The root wrote separate score, support, protocol, hypothesis-family, immutable-input and publication tests before its runner implementation.

The first missing-module test failures are retained. The full generated pipeline includes an entirely unscored first application month, a complete month with unknown original CPI-plan features, source rows whose later target is still known, and all model fits before the future query-label mask. This distinguishes missing source information from missing future outcomes and checks every unscored monthly fit.

The integration review found a training-mask discrepancy: a generated first row had finite old feature values but no preceding-session cutoff. The independent mask excluded it; the new producer did not. A direct regression was written and failed before adding the known-cutoff requirement. This strengthened timing admission before any empirical run. No row was filled and no source window changed.

A separate evaluator regression found that a corrupted scored row with an unknown old macro feature could reach inference. The new evaluator now rejects every incomplete, nonnumeric or complex full predictor vector at scored origins before support and scoring, without filtering rows. The independent forecast replay also enforces the complete common mask.

The independent native-score inference check found that a reused helper assumed nonnegative squared losses. Proper variance scores can legitimately be negative. A direct synthetic test preserved that failure, after which the new runner received its own signed-score inference function with the same fixed normalized-difference, bootstrap and HAC calculations. It retains native mean scores without shifting them and computes the declared .005-effect power diagnostic. The stable paired score function already enforces the prospective component-level coherence envelope; the unrelated old squared-loss envelope is not reused. Independent inference reconstructs all four phase analyses using its own implementations.

The modeling reviewer independently re-derived the proper-score gap, including both log1p signs, the wide-gap branch, conditional-mean elicitation, common-unit invariance, identical forecasts, native subnormal values and nonzero-underflow rejection. It found no material issue in the score or protocol. Fixed gradients, brackets, penalties, support thresholds, novelty and rank tolerances were not chosen from empirical values.

The source admission reconstructs the entire original VERIFIED records of both causal_pool and calendar_variance through frozen read-only functions. The older raw calendar reconstruction reads a temporary same-relative-path tree of hash-checked original input bytes; only documentary source_path prefixes are restored for comparison. It does not invoke historical writers or compare historical code inventories to the expanded current tree. The current manifest covers both old input inventories and every previous output/report byte.

The final pre-fit gate is a passing full repository suite, focused pre-run suite, new-file lint, and unchanged prior pins. Exact results and frozen code/protocol/design hashes are saved in the freeze record and run manifest. Any later source, support, fitting, scoring, verification or publication failure leaves both new hypotheses unevaluable with p=1. No empirical parameter repair, fold deletion, alternative fitting restart or gate relaxation is authorized by this protocol. Both control comparisons and every fixed gate are required for a candidate to qualify; reused historical evidence remains exploratory.
