# Independent prospective execution review — joint copula

Status: **NO_REMAINING_BLOCKER_IN_BOUNDED_PREFIT_REVIEW**. This source, mathematical and generated-evidence review is complete before wave27 registration. The root agent may now run the prescribed selected repository suite and freeze the final complete inventory. This review does not assert that the historical experiment has run or passed.

Reviewer: new_study_tests. Scope comprises the new source reader, exact protocol projection, monthly producer integration, independent forecast/certificate verification, and actual registration/publication path. The separate numerical review in `NUMERICAL_EXECUTION_REVIEW.md` was read as supporting evidence; it does not replace this execution inspection. No historical numerical source, support, fit, issued forecast or score was decoded here. No whole-repository test suite, source admission, empirical pipeline, calibration or market resampling was run by this reviewer. Source/test files outside the assigned new verifier remained untouched. Generated fixtures, saved code, metadata and pre-existing methodological reports were used.

## Scientific and clock agreement

The new question is a joint next-session return density, with the same fixed t8 conditional marginals across all three arms. It is not presented as another successful volatility signal or a new external information source. Both separately fitted dependence controls receive the same current-fit staged residual pairs; rho0 in the t8 copula is correctly distinct from independence. The fixed-domain numerical certificate establishes an approximate global objective bound and projected stationarity, independently of a local optimizer's success flag.

The exact six-field configuration agrees across protocol, pipeline and verifier. Origin requests span2016-01-04..2025-10-17; the full numerical/reference ceiling is2025-10-20. Development ends2019-12-31 and evaluation's literal domain is2020-01-01..2025-10-20. The calendar retains final unissued/immature and missing positions where required for inference. Shared34-feature completeness selects each monthly first query without its label. Training uses all common earlier rows whose paired next-SPX-session outcomes mature by the fit's preceding full-calendar session. All complete requested applications are issued and retained, including missing-target and phase-end applications. Strict source predecessor, measurement, scale and no-fallback rules remain inherited.

The reused independent marginal oracle has several distinct fixed tolerances. The initial uniform execution metadata did not accurately describe them; before freeze the root replaced it with `marginal_oracle_tolerances`. The independent verifier's direct saved-output comparison was tightened prospectively to absolute1e-12/relative1e-7, while inherited solver/oracle tolerances remain unchanged. Generated RED tests also established explicit strict variance/rho domains, exact cross-arm shared marginals, a literal zero independence placeholder and constant monthly parameters even on unscored origins. `FORECAST_VERIFIER_CONTRACT.md` describes those corrections, the independent formulas and the honest initial failing evidence. No observed historical value motivated a tolerance or model change.

## Source admission and registration order

`read_sources` requires exactly the original six paths and their SHA256 values. It captures all raw byte buffers, verifies every hash, validates every full source date column, and only then converts the bounded numerical columns from those same buffers. The CSV post-ceiling values remain strings; bounded parquet reads request only the declared raw OHLC fields. Adjusted close, volume, alternate snapshots and Treasury quantities are not substituted. The source reader's generated tests explicitly exercise a changed source after buffer capture, a bad final hash and malformed later date before any numerical callback.

The actual runner checks the original protocol hash, full current source/test inventory, exact mandatory source/proposal pins, ancestral preservation maps and selected-test receipts before proceeding. The saved root source-closure preflight reports30,050 source/ancestor pins and149 inherited identities with numerical_data_decoded=false. This review inspected that metadata report and the checking path; it did not independently rerun its30,050 hash operations or treat it as a numerical admission.

The two registrations and all149 inherited dictionaries are durably written before the callback that invokes the reader. Both new identities are fixed: t8_copula versus gaussian_copula and independence, horizon1, negative_joint_log_density. Inheritance is copied unchanged and later mutable caller changes cannot alter the recorded family. A complete ordinary journal is prospectively153 rows: two registrations,149 inheritances and two evaluations. Cumulative multiplicity is151. No such count is claimed as an observed empirical result in this review.

The complete reference measurement audit is saved before requiring its gate to pass, and before feature/label selection. Source, measurement, support and numerical failures make both comparisons unevaluable with probability one. A registered attempt is neither restarted nor replaced by another model or cohort. Pipeline exceptions preserve available diagnostics and earlier issued records; the failure path does not promote a partial scored panel.

## Saved-output and publication binding

The actual runner preserves the three binding corrections introduced before wave26, with the new paths/family substituted. It hashes the freeze-record payload before decoding and keeps that original signature. Each of the seven data files is pinned after its first save and the exact inventory is checked. It then reloads the actual six Parquet files and fits JSON for independent reconstruction; verification no longer concerns only the original in-memory output. The verifier's generated serialized roundtrip passes, including nullable counts and native dates.

Both forecast/certificate and score verifiers must return VERIFIED. The output pins are checked before verification and again before its combined certificate and around metrics and terminal publication. Reports bind their intended JSON serialization, rather than accepting a fresh post-mutation hash; the journal binds the original payload and every append against its previously checked prefix. The terminal is itself saved through that bound path and checked afterward. An altered saved output, freeze, metrics or proof cannot remain COMPLETED merely because terminal() freshly hashes it. Provisional evidence is retained additively on invalidation. Earlier frozen runner implementations are unchanged.

The new registration tests preserve the existing prewritten cases for durable ordering, invalid family, replay refusal, caller mutation, missing/reordered controls, journal failure, changed freeze bytes, output drift, metrics mutation and late preservation failure. This review also read the actual `run` composition to confirm that those helpers are invoked at the declared boundaries; helper tests alone would not establish that wiring.

## Statistical composition and evidence

The scorer forms paired differences directly as control-minus-candidate copula log density while retaining full joint negative log densities for the reported model means. The shared marginal terms cancel algebraically, and negative continuous-density losses remain valid. Both scorers use the original literal phase calendars, missing-position masks and global modulo-five groups. Support505/252/63 is enforced before resampling. Both comparisons share the same within-phase random design and require the specified maximum phase/method probability, Holm2 and Holm151 thresholds, effect<=-.005 in each phase, and strictly negative slice/offset gaps. A single-control pass cannot emit a lead.

The independent scorer reconstructs its own densities and indexed inference; its explicit producer-density agreement call checks the producer against already independently computed values rather than using those values as the reconstruction oracle. Continuous probability tolerances and exact bootstrap count probabilities follow the fixed new protocol. Reused synthetic daily-mask calibration is a diagnostic of that generated process, not certification of market coverage or extreme multiplicity-adjusted tail probabilities.

Completed generated evidence inspected:24 independent math/forecast tests passed in1.638 seconds with clean Ruff/formatting;18 producer model/pipeline/input tests passed in0.907 seconds;15 density tests passed in0.328 seconds; the root's combined13 execution/protocol plus15 density tests passed as28 tests in0.738 seconds. Scoring first-implementation logs record11 producer and six independent tests passing. These are component results, not the forthcoming selected-suite receipt. No additional legacy replay tests may be quarantined beyond the six explicitly disclosed IDs, and ordinary skipped tests cannot be treated as execution.

The root protocol carries the reused-history/current-vintage and prior post2025-11-03 test-access qualifications, including QQQ ETF versus SPX price-index identity and uncertified synchronization/publication latency. This study's2025-10-20 numerical ceiling does not make later history untouched. Even a fully verified numerical pass would remain an exploratory joint-density result with potential shared-marginal misspecification, not proof of pure tail dependence, index direction, better standalone volatility or tradable profit.

The forthcoming freeze must bind the complete final inventory and test receipts. This review is limited to the code and generated evidence below. Empirical results, final publication audits and future rendering artifacts are outside its scope.

## Reviewed byte identities

| File | SHA-256 |
| --- | --- |
| `joint_copula.yaml` | `cf43700c6c37ba860e9ef8ae4adb4c70158030efeedd3bca9b073f0691cd5d50` |
| `src/joint_copula_protocol.py` | `1ccca48122f36f74a684909126ae71e4586c2fbb4ab732688865e0da1c72f5c2` |
| `src/joint_copula_inputs.py` | `1d6ef58f203682dae6dbdf1f55ac9a50f34308c05bcd647d63dab385bb689cec` |
| `src/joint_copula_search.py` | `fe059824f09de9514d1538f6f12c8229e43eee7305afcf96ac853b3f55bff9aa` |
| `tests/test_joint_copula_search.py` | `90f36a3b382da1b646e5d98e43be91f6834b021f55ea16a69302393341a9dab7` |
| `tests/test_joint_copula_inputs.py` | `d3bf6f3ef583527cf224e7af051af1a8af1f281070c147443a88591a02b9e308` |
| `src/joint_copula_models.py` | `4ab1eb02fe7703cf8639f062e9fb976f7ee22ddd4cc13451566c0e4660558c86` |
| `src/joint_copula_pipeline.py` | `0ea290b36f15adfb5cc59147ae7132848358ff84795f3c6008a2e089bc8eecac` |
| `src/joint_copula_density.py` | `da502a656cf4ae480ea6377a546f12645442680bb922ed5c45b2c2bde3e48a0e` |
| `src/verify_joint_copula_forecasts.py` | `e74326da39c31de64140cedb0f38530a297680bc313e1777942b777cda25060c` |
| `tests/test_verify_joint_copula_forecasts.py` | `7b9c765db70cfd6dbc070a6d9924f469cb8006c43f13e032449e22e2a8920e76` |
| `src/joint_copula_score.py` | `f5ae136480f4f96c18c1bb5e36206fbce7a1d3447f9f0c63b75fa18bd4cbc22c` |
| `src/verify_joint_copula_scores.py` | `3d3955d2e16b39c6f6703e62938fbb974e5df2a1621f358a4052192c85d3b492` |
| `reports/joint_copula/prefit/FORECAST_VERIFIER_CONTRACT.md` | `d8084a80ace6d9e8ff2aa7a45bf8d9b797c5c6133d1b4d299273caf9e612bb16` |
| `reports/joint_copula/prefit/FORECAST_VERIFIER_FINAL_GREEN.log` | `58593cf2c99095408b55555d2ac23fdc2b6661e86f6246b7d19ed6c777aa1135` |
| `reports/joint_copula/prefit/ROOT_INTEGRATION_GREEN.log` | `c0acf68cddea75ff003ffc152eab9a8e16de6d1b16880648b245b87d38699779` |
| `reports/joint_copula/prefit/PRODUCER_FINAL_GREEN.log` | `2afecaee6a97064e5587a5204d5177fd99e05588e5d55c3c94710912b0c99067` |
| `reports/joint_copula/prefit/SCORE_FIRST_IMPLEMENTATION.log` | `65f58d2675db43563a4959c75d825c15b18de705a20aaa8d72cd07d378e94fa4` |
| `reports/joint_copula/prefit/SCORE_VERIFICATION_FIRST_IMPLEMENTATION.log` | `e684a075bb781a68344af841b22c7dd4653c658bb17c8168031b95aa85138b3e` |
| `reports/joint_copula/prefit/NUMERICAL_EXECUTION_REVIEW.md` | `78378830ee19ca07d31f26e2c25d6d4b9edf5d85a2c81a9cc63f459b109e0e0c` |
| `reports/joint_copula/prefit/SOURCE_CLOSURE_PREFLIGHT.json` | `dcf8ad45afd370653a218393ac65d9af76fb9f0765b7a53b729b9b4f679d12a2` |
